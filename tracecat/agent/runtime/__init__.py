"""Agent runtimes for sandboxed execution.

This package contains runtime implementations that can be loaded dynamically
by the sandbox entrypoint based on the runtime_type in the init payload.

Available runtimes:
- claude_code: Claude Agent SDK runtime (ClaudeAgentRuntime)
"""
