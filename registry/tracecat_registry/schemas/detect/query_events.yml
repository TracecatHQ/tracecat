type: object
properties:
  start_time:
    type: datetime
    description: Start time for the query (inclusive) in ISO 8601 format.
  end_time:
    type: datetime
    description: End time for the query (exclusive) in ISO 8601 format.
  query:
    type: str
    description: (Optional) Query or filter to apply to log events. If specified, overrides `start_time` and `end_time`.
    default: null
  limit:
    type: int
    description: Maximum number of log events to return. Defaults to 100.
    default: 100
required:
  - start_time
  - end_time
