---
title: "AI preset agent"
description: "Save reusable agent configurations with instructions, tools, skills, and MCP integrations, then run them from workflows and workspace chat."
---

<Badge icon="github" color="gray" size="lg" shape="pill">Open source</Badge>

Agent presets save a model, instructions, tools, skills, and MCP integrations under a slug.
Open `Agents` from the workspace navigation to create one, then run it from a workflow with `ai.preset_agent`.

![Agents page](/img/agents/agents-list.png)

## Create a preset

Click `Create new` and select `Agent`. Enter a name and optional description, then click `Create agent`.
The preset starts with your organization default model. You can change its model after creation.

![Create agent dialog with a name and optional description](/img/agents/create-agent.jpg)

### Set up the default model

Your organization default model must be enabled for your workspace before you can create a preset.

If you see `Set up model provider`, click `Configure models` to open `Organization > Settings > Agent`. Connect a [provider](/agents/llm-providers) and choose a default model. If you cannot configure organization settings, ask an organization administrator to do this.

![Set up model provider prompt with Configure models button](/img/agents/create-agent-set-up-model.jpg)

If you see `Enable the default model`, click `Configure workspace models` and enable the organization default model in workspace AI model settings. If you cannot configure workspace settings, ask a workspace administrator to enable it.

![Enable the default model prompt with Configure workspace models button](/img/agents/create-agent-enable-model.jpg)

After updating the model settings, return to `Agents` and select `Create new > Agent`.

## Configure a preset

Write instructions in the editor.
Use the `Tools` tab to select the model, allowed Tracecat actions, and MCP integrations, and the `Skills` tab to attach published [skills](/agents/skills).

![Agent preset tools](/img/agents/agent-preset-tools.png)

## Where tools come from

Three sources on the preset side make up what the agent can call:

- **Registry actions** on the preset, chosen in the `Tools` tab. The usual home for
  actions only this agent needs.
- **MCP integrations** attached to the preset, also in the `Tools` tab. An MCP
  integration reaches a preset directly and does not need to go through a skill.
- **A skill's [`metadata.tools`](/agents/skills#declare-tools-in-frontmatter)**, which
  takes registry actions and MCP references alike. Tools declared here travel with the
  skill's instructions, so a group several agents reuse stays in one place.

The effective tool set is the union of the three. A `namespaces` restriction on the preset
filters the registry actions in that set; MCP tools are not filtered by it.

A workflow run can swap one piece out. The `actions` input on the `ai.preset_agent` action
replaces the preset's registry actions for that run instead of adding to them, so registry
actions a skill contributed are dropped for the run. MCP tools stay either way, and an
empty list is ignored. It is handy for running a preset with a different tool set in a
test or an eval.

Leave `output_type` unset unless you specifically need structured output for a downstream step.

## Availability

Open source includes presets, [skills](/agents/skills), [secrets and variables](/agents/secrets-variables) in preset instructions, and the MCP catalog connectors.

<Badge icon="lock" color="blue" size="lg" shape="pill">Enterprise Edition</Badge>

Enterprise adds:

- Agent folders and tags to organize the `Agents` page.
- `tool_approvals` and the agent inbox to review tool calls before they run.
- `@` agent mentions in [workspace chat](/agents/workspace-chat#mention-an-agent) and [case comments](/automations/cases/agent-mentions).

## Related pages

- See [AI agent](/agents/ai-agent#ai-preset-agent) for the `ai.preset_agent` inputs and examples.
- See [Skills](/agents/skills) to attach reusable instructions and files to a preset.
- See [MCP servers](/automations/integrations/mcp-integrations) to connect servers a preset can call.
