---
title: "HTTP"
---

{/* Auto-generated by scripts/generate_core_action_docs.py; do not edit by hand. */}

## `core.http_request`

Perform a HTTP request to a given URL.

### Inputs

<ResponseField name="method" type="string">

Required.

HTTP request method

Allowed values: `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, `HEAD`, `OPTIONS`.

</ResponseField>

<ResponseField name="url" type="string">

Required.

The destination of the HTTP request

</ResponseField>

<ResponseField name="auth" type="map[string, string] | null">

Optional.

Basic auth credentials with `username` and `password` keys

Default: `null`.

</ResponseField>

<ResponseField name="base64_encode_data" type="boolean">

Optional.

Base64 encode the raw response body before returning. Use this for binary downloads to prevent corruption from text decoding.

Default: `false`.

</ResponseField>

<ResponseField name="files" type="map[string, string | FileUploadData] | null">

Optional.

Files to upload using multipart/form-data. The dictionary key is the form field name. The value can be a simple base64 encoded string (filename defaults to form field name), or a dictionary with 'filename', 'content_base64', and optional 'content_type'.

Default: `null`.

</ResponseField>

<ResponseField name="follow_redirects" type="boolean">

Optional.

Follow HTTP redirects

Default: `false`.

</ResponseField>

<ResponseField name="form_data" type="object | null">

Optional.

Form encoded data in request body (POST, PUT, and PATCH)

Default: `null`.

</ResponseField>

<ResponseField name="headers" type="map[string, string] | null">

Optional.

HTTP request headers

Default: `null`.

</ResponseField>

<ResponseField name="ignore_status_codes" type="array[integer] | null">

Optional.

If specified, these status codes will not be treated as errors. Defaults to None.

Default: `null`.

</ResponseField>

<ResponseField name="max_redirects" type="integer">

Optional.

Maximum number of redirects

Default: `20`.

</ResponseField>

<ResponseField name="params" type="object | null">

Optional.

URL query parameters

Default: `null`.

</ResponseField>

<ResponseField name="payload" type="object | array[any] | null">

Optional.

JSON serializable data in request body (POST, PUT, and PATCH)

Default: `null`.

</ResponseField>

<ResponseField name="timeout" type="number">

Optional.

Timeout in seconds

Default: `10.0`.

</ResponseField>

<ResponseField name="verify_ssl" type="boolean">

Optional.

Verify SSL certificates. Defaults to True, disable at own risk.

Default: `true`.

</ResponseField>

### Examples

**Basic request**

```yaml
- ref: fetch_alert
  action: core.http_request
  args:
    url: https://api.example.com/alerts/${{ TRIGGER.alert_id }}
    method: GET
    headers:
      Authorization: Bearer ${{ SECRETS.alerts.API_TOKEN }}
```

## `core.http_poll`

Perform a HTTP request to a given URL with optional polling.

### Inputs

<ResponseField name="method" type="string">

Required.

HTTP request method

Allowed values: `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, `HEAD`, `OPTIONS`.

</ResponseField>

<ResponseField name="url" type="string">

Required.

The destination of the HTTP request

</ResponseField>

<ResponseField name="auth" type="map[string, string] | null">

Optional.

Basic auth credentials with `username` and `password` keys

Default: `null`.

</ResponseField>

<ResponseField name="follow_redirects" type="boolean">

Optional.

Follow HTTP redirects

Default: `false`.

</ResponseField>

<ResponseField name="form_data" type="object | null">

Optional.

Form encoded data in request body (POST, PUT, and PATCH)

Default: `null`.

</ResponseField>

<ResponseField name="headers" type="map[string, string] | null">

Optional.

HTTP request headers

Default: `null`.

</ResponseField>

<ResponseField name="max_redirects" type="integer">

Optional.

Maximum number of redirects

Default: `20`.

</ResponseField>

<ResponseField name="params" type="object | null">

Optional.

URL query parameters

Default: `null`.

</ResponseField>

<ResponseField name="payload" type="object | array[any] | null">

Optional.

JSON serializable data in request body (POST, PUT, and PATCH)

Default: `null`.

</ResponseField>

<ResponseField name="poll_condition" type="string | null">

Optional.

Python lambda function when evaluated to True, stops polling. The function receives a dict with `headers`, `data`, and `status_code` fields.

Default: `null`.

</ResponseField>

<ResponseField name="poll_interval" type="number | null">

Optional.

Interval in seconds between polling attempts. If not specified, defaults to polling with exponential wait.

Default: `null`.

</ResponseField>

<ResponseField name="poll_max_attempts" type="integer">

Optional.

Maximum number of polling attempts. If set to 0, the action will poll indefinitely (until timeout).

Default: `10`.

</ResponseField>

<ResponseField name="poll_retry_codes" type="integer | array[integer] | null">

Optional.

Status codes on which the action will retry. Ignored if `poll_condition` is provided. If neither are specified, an error will be raised.

Default: `null`.

</ResponseField>

<ResponseField name="timeout" type="number">

Optional.

Timeout in seconds

Default: `10.0`.

</ResponseField>

<ResponseField name="verify_ssl" type="boolean">

Optional.

Verify SSL certificates. Defaults to True, disable at own risk.

Default: `true`.

</ResponseField>

### Examples

**Poll until complete**

```yaml
- ref: wait_for_export
  action: core.http_poll
  args:
    url: https://api.example.com/exports/${{ TRIGGER.export_id }}
    method: GET
    headers:
      Authorization: Bearer ${{ SECRETS.exports.API_TOKEN }}
    poll_interval: 5
    poll_max_attempts: 24
    poll_condition: "lambda response: response['data'].get('status') == 'completed'"
```

## `core.http_paginate`

Paginate through a HTTP response.

### Inputs

<ResponseField name="method" type="string">

Required.

HTTP request method

Allowed values: `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, `HEAD`, `OPTIONS`.

</ResponseField>

<ResponseField name="next_request" type="string">

Required.

Python lambda function that returns the next request as a JSON of `url`, `method`, `headers`, `params`, `payload`, `form_data` to paginate to. The function receives a dict with `headers`, `data`, and `status_code` fields.

</ResponseField>

<ResponseField name="stop_condition" type="string">

Required.

Python lambda function that determines when pagination should STOP. The function receives a dict with `headers`, `data`, and `status_code` fields.

</ResponseField>

<ResponseField name="url" type="string">

Required.

The destination of the HTTP request

</ResponseField>

<ResponseField name="auth" type="map[string, string] | null">

Optional.

Basic auth credentials with `username` and `password` keys

Default: `null`.

</ResponseField>

<ResponseField name="follow_redirects" type="boolean">

Optional.

Follow HTTP redirects

Default: `false`.

</ResponseField>

<ResponseField name="form_data" type="object | null">

Optional.

Form encoded data in request body (POST, PUT, and PATCH)

Default: `null`.

</ResponseField>

<ResponseField name="headers" type="map[string, string] | null">

Optional.

HTTP request headers

Default: `null`.

</ResponseField>

<ResponseField name="items_jsonpath" type="string | null">

Optional.

JSONPath expression that evaluates to the items to paginate through.

Default: `null`.

</ResponseField>

<ResponseField name="limit" type="integer">

Optional.

Maximum number of items to paginate through. Defaults to 1000.

Default: `1000`.

</ResponseField>

<ResponseField name="max_redirects" type="integer">

Optional.

Maximum number of redirects

Default: `20`.

</ResponseField>

<ResponseField name="params" type="object | null">

Optional.

URL query parameters

Default: `null`.

</ResponseField>

<ResponseField name="payload" type="object | array[any] | null">

Optional.

JSON serializable data in request body (POST, PUT, and PATCH)

Default: `null`.

</ResponseField>

<ResponseField name="timeout" type="number">

Optional.

Timeout in seconds

Default: `10.0`.

</ResponseField>

<ResponseField name="verify_ssl" type="boolean">

Optional.

Verify SSL certificates. Defaults to True, disable at own risk.

Default: `true`.

</ResponseField>

### Examples

**Follow next page links**

```yaml
- ref: list_findings
  action: core.http_paginate
  args:
    url: https://api.example.com/findings
    method: GET
    headers:
      Authorization: Bearer ${{ SECRETS.findings.API_TOKEN }}
    params:
      limit: 100
    items_jsonpath: $.items[*]
    stop_condition: "lambda response: response['data'].get('next_cursor') is None"
    next_request: "lambda response: {'url': 'https://api.example.com/findings', 'method': 'GET', 'headers': {'Authorization': 'Bearer ${{ SECRETS.findings.API_TOKEN }}'}, 'params': {'limit': 100, 'cursor': response['data']['next_cursor']}}"
```
