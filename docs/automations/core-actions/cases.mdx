---
title: "Cases"
---

{/* Auto-generated by scripts/generate_core_action_docs.py; do not edit by hand. */}

## `core.cases.create_case`

Create a new case.

### Inputs

<ResponseField name="description" type="string">

Required.

The description of the case.

</ResponseField>

<ResponseField name="summary" type="string">

Required.

The summary of the case.

</ResponseField>

<ResponseField name="dropdown_values" type="array[CaseDropdownValueInput] | null">

Optional.

Dropdown selections to set on the case. Each item must include either definition_id or definition_ref, and either option_id or option_ref (or null to clear).

Default: `null`.

</ResponseField>

<ResponseField name="fields" type="object | null">

Optional.

Custom fields for the case.

Default: `null`.

</ResponseField>

<ResponseField name="payload" type="object | null">

Optional.

Payload for the case.

Default: `null`.

</ResponseField>

<ResponseField name="priority" type="string">

Optional.

The priority of the case.

Default: `"unknown"`.

Allowed values: `unknown`, `low`, `medium`, `high`, `critical`, `other`.

</ResponseField>

<ResponseField name="severity" type="string">

Optional.

The severity of the case.

Default: `"unknown"`.

Allowed values: `unknown`, `informational`, `low`, `medium`, `high`, `critical`, `fatal`, `other`.

</ResponseField>

<ResponseField name="status" type="string">

Optional.

The status of the case.

Default: `"unknown"`.

Allowed values: `unknown`, `new`, `in_progress`, `on_hold`, `resolved`, `closed`, `other`.

</ResponseField>

<ResponseField name="tags" type="array[string] | null">

Optional.

List of tag identifiers (IDs or refs) to add to the case.

Default: `null`.

</ResponseField>

### Examples

**Create and fetch a case**

```yaml
- ref: create_case
  action: core.cases.create_case
  args:
    summary: "Investigate alert ${{ TRIGGER.alert_id }}"
    description: "Created from workflow trigger."
    priority: high
    severity: high
    tags:
      - phishing
- ref: get_case
  action: core.cases.get_case
  depends_on:
    - create_case
  args:
    case_id: ${{ ACTIONS.create_case.result.id }}
```

## `core.cases.get_case`

Get details of a specific case by ID.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to retrieve.

</ResponseField>

### Examples

**Create and fetch a case**

```yaml
- ref: create_case
  action: core.cases.create_case
  args:
    summary: "Investigate alert ${{ TRIGGER.alert_id }}"
    description: "Created from workflow trigger."
    priority: high
    severity: high
    tags:
      - phishing
- ref: get_case
  action: core.cases.get_case
  depends_on:
    - create_case
  args:
    case_id: ${{ ACTIONS.create_case.result.id }}
```

## `core.cases.update_case`

Update an existing case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to update.

</ResponseField>

<ResponseField name="append" type="boolean">

Optional.

If true, append the provided description to the existing description when it is not empty.

Default: `false`.

</ResponseField>

<ResponseField name="description" type="string | null">

Optional.

The updated description of the case.

Default: `null`.

</ResponseField>

<ResponseField name="dropdown_values" type="array[CaseDropdownValueInput] | null">

Optional.

Dropdown selections to set or clear. Each item must include either definition_id or definition_ref, and either option_id or option_ref (or null to clear).

Default: `null`.

</ResponseField>

<ResponseField name="fields" type="object | null">

Optional.

Updated custom fields for the case.

Default: `null`.

</ResponseField>

<ResponseField name="payload" type="object | null">

Optional.

Updated payload for the case.

Default: `null`.

</ResponseField>

<ResponseField name="priority" type="string | null">

Optional.

The updated priority of the case.

Default: `null`.

</ResponseField>

<ResponseField name="severity" type="string | null">

Optional.

The updated severity of the case.

Default: `null`.

</ResponseField>

<ResponseField name="status" type="string | null">

Optional.

The updated status of the case.

Default: `null`.

</ResponseField>

<ResponseField name="summary" type="string | null">

Optional.

The updated summary of the case.

Default: `null`.

</ResponseField>

<ResponseField name="tags" type="array[string] | null">

Optional.

List of tag identifiers (IDs or refs) to set on the case. This will replace all existing tags.

Default: `null`.

</ResponseField>

### Examples

**Update case fields**

```yaml
- ref: update_case
  action: core.cases.update_case
  args:
    case_id: ${{ TRIGGER.case_id }}
    status: in_progress
    priority: critical
    append: true
    description: |
      Analyst note:
      - Escalated after credential theft indicators were confirmed.
```

## `core.cases.list_cases`

List all cases.

### Inputs

<ResponseField name="cursor" type="string | null">

Optional.

Pagination cursor used to fetch a specific page when paginate=true.

Default: `null`.

</ResponseField>

<ResponseField name="limit" type="integer">

Optional.

Maximum number of cases to return.

Default: `100`.

</ResponseField>

<ResponseField name="order_by" type="string | null">

Optional.

The field to order the cases by.

Default: `null`.

</ResponseField>

<ResponseField name="paginate" type="boolean">

Optional.

If true, return cursor pagination metadata along with items.

Default: `false`.

</ResponseField>

<ResponseField name="reverse" type="boolean">

Optional.

Reverse pagination direction when paginate=true.

Default: `false`.

</ResponseField>

<ResponseField name="sort" type="string | null">

Optional.

The direction to order the cases by.

Default: `null`.

</ResponseField>

### Examples

**List, search, and inspect case activity**

```yaml
- ref: list_cases
  action: core.cases.list_cases
  args:
    limit: 25
    order_by: updated_at
    sort: desc
- ref: search_cases
  action: core.cases.search_cases
  args:
    search_term: phishing
    status:
      - new
      - in_progress
    limit: 25
- ref: case_events
  action: core.cases.list_case_events
  args:
    case_id: ${{ TRIGGER.case_id }}
```

## `core.cases.search_cases`

Search cases based on various criteria.

### Inputs

<ResponseField name="assignee_id" type="string | array[string] | null">

Optional.

Filter by assignee ID or 'unassigned'.

Default: `null`.

</ResponseField>

<ResponseField name="cursor" type="string | null">

Optional.

Pagination cursor used to fetch a specific page when paginate=true.

Default: `null`.

</ResponseField>

<ResponseField name="dropdown" type="array[string] | null">

Optional.

Filter by dropdown values in definition_ref:option_ref format.

Default: `null`.

</ResponseField>

<ResponseField name="end_time" type="string | null">

Optional.

Filter cases created before this time.

Default: `null`.

</ResponseField>

<ResponseField name="limit" type="integer">

Optional.

Maximum number of cases to return.

Default: `100`.

</ResponseField>

<ResponseField name="order_by" type="string | null">

Optional.

The field to order the cases by.

Default: `null`.

</ResponseField>

<ResponseField name="paginate" type="boolean">

Optional.

If true, return cursor pagination metadata along with items.

Default: `false`.

</ResponseField>

<ResponseField name="priority" type="string | array[string] | null">

Optional.

Filter by case priority.

Default: `null`.

</ResponseField>

<ResponseField name="reverse" type="boolean">

Optional.

Reverse pagination direction when paginate=true.

Default: `false`.

</ResponseField>

<ResponseField name="search_term" type="string | null">

Optional.

Text to search for in case summary and description.

Default: `null`.

</ResponseField>

<ResponseField name="severity" type="string | array[string] | null">

Optional.

Filter by case severity.

Default: `null`.

</ResponseField>

<ResponseField name="sort" type="string | null">

Optional.

The direction to order the cases by.

Default: `null`.

</ResponseField>

<ResponseField name="start_time" type="string | null">

Optional.

Filter cases created after this time.

Default: `null`.

</ResponseField>

<ResponseField name="status" type="string | array[string] | null">

Optional.

Filter by case status.

Default: `null`.

</ResponseField>

<ResponseField name="tags" type="array[string] | null">

Optional.

Filter by tag IDs or refs (AND logic).

Default: `null`.

</ResponseField>

<ResponseField name="updated_after" type="string | null">

Optional.

Filter cases updated after this time.

Default: `null`.

</ResponseField>

<ResponseField name="updated_before" type="string | null">

Optional.

Filter cases updated before this time.

Default: `null`.

</ResponseField>

### Examples

**List, search, and inspect case activity**

```yaml
- ref: list_cases
  action: core.cases.list_cases
  args:
    limit: 25
    order_by: updated_at
    sort: desc
- ref: search_cases
  action: core.cases.search_cases
  args:
    search_term: phishing
    status:
      - new
      - in_progress
    limit: 25
- ref: case_events
  action: core.cases.list_case_events
  args:
    case_id: ${{ TRIGGER.case_id }}
```

## `core.cases.list_case_events`

List all events for a case in chronological order.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to get events for.

</ResponseField>

### Examples

**List, search, and inspect case activity**

```yaml
- ref: list_cases
  action: core.cases.list_cases
  args:
    limit: 25
    order_by: updated_at
    sort: desc
- ref: search_cases
  action: core.cases.search_cases
  args:
    search_term: phishing
    status:
      - new
      - in_progress
    limit: 25
- ref: case_events
  action: core.cases.list_case_events
  args:
    case_id: ${{ TRIGGER.case_id }}
```

## `core.cases.create_comment`

Add a comment to an existing case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to comment on.

</ResponseField>

<ResponseField name="content" type="string">

Required.

The comment content.

</ResponseField>

<ResponseField name="parent_id" type="string | null">

Optional.

The ID of the parent comment if this is a reply.

Default: `null`.

</ResponseField>

<ResponseField name="workflow_id" type="string | null">

Optional.

The ID of the workflow to run when the comment is created.

Default: `null`.

</ResponseField>

### Examples

**Manage case comments**

```yaml
- ref: create_comment
  action: core.cases.create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    content: "Initial triage started."
- ref: reply_to_comment
  action: core.cases.reply_to_comment
  depends_on:
    - create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    parent_comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Added IOC context."
- ref: update_comment
  action: core.cases.update_comment
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Initial triage started. Waiting on endpoint data."
- ref: list_comments
  action: core.cases.list_comments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: list_comment_threads
  action: core.cases.list_comment_threads
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_comment_thread
  action: core.cases.get_comment_thread
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
```

## `core.cases.reply_to_comment`

Reply to a top-level case comment.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case containing the parent comment.

</ResponseField>

<ResponseField name="content" type="string">

Required.

The reply content.

</ResponseField>

<ResponseField name="parent_comment_id" type="string">

Required.

The ID of the top-level comment to reply to.

</ResponseField>

### Examples

**Manage case comments**

```yaml
- ref: create_comment
  action: core.cases.create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    content: "Initial triage started."
- ref: reply_to_comment
  action: core.cases.reply_to_comment
  depends_on:
    - create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    parent_comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Added IOC context."
- ref: update_comment
  action: core.cases.update_comment
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Initial triage started. Waiting on endpoint data."
- ref: list_comments
  action: core.cases.list_comments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: list_comment_threads
  action: core.cases.list_comment_threads
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_comment_thread
  action: core.cases.get_comment_thread
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
```

## `core.cases.update_comment`

Update an existing case comment.

### Inputs

<ResponseField name="comment_id" type="string">

Required.

The ID of the comment to update.

</ResponseField>

<ResponseField name="content" type="string">

Required.

The updated comment content.

</ResponseField>

### Examples

**Manage case comments**

```yaml
- ref: create_comment
  action: core.cases.create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    content: "Initial triage started."
- ref: reply_to_comment
  action: core.cases.reply_to_comment
  depends_on:
    - create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    parent_comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Added IOC context."
- ref: update_comment
  action: core.cases.update_comment
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Initial triage started. Waiting on endpoint data."
- ref: list_comments
  action: core.cases.list_comments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: list_comment_threads
  action: core.cases.list_comment_threads
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_comment_thread
  action: core.cases.get_comment_thread
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
```

## `core.cases.list_comments`

List all comments for a case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to get comments for.

</ResponseField>

### Examples

**Manage case comments**

```yaml
- ref: create_comment
  action: core.cases.create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    content: "Initial triage started."
- ref: reply_to_comment
  action: core.cases.reply_to_comment
  depends_on:
    - create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    parent_comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Added IOC context."
- ref: update_comment
  action: core.cases.update_comment
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Initial triage started. Waiting on endpoint data."
- ref: list_comments
  action: core.cases.list_comments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: list_comment_threads
  action: core.cases.list_comment_threads
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_comment_thread
  action: core.cases.get_comment_thread
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
```

## `core.cases.list_comment_threads`

List comment threads for a case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to get comment threads for.

</ResponseField>

### Examples

**Manage case comments**

```yaml
- ref: create_comment
  action: core.cases.create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    content: "Initial triage started."
- ref: reply_to_comment
  action: core.cases.reply_to_comment
  depends_on:
    - create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    parent_comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Added IOC context."
- ref: update_comment
  action: core.cases.update_comment
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Initial triage started. Waiting on endpoint data."
- ref: list_comments
  action: core.cases.list_comments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: list_comment_threads
  action: core.cases.list_comment_threads
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_comment_thread
  action: core.cases.get_comment_thread
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
```

## `core.cases.get_comment_thread`

Get the full thread for a comment ID.

### Inputs

<ResponseField name="comment_id" type="string">

Required.

The ID of a comment within the thread.

</ResponseField>

### Examples

**Manage case comments**

```yaml
- ref: create_comment
  action: core.cases.create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    content: "Initial triage started."
- ref: reply_to_comment
  action: core.cases.reply_to_comment
  depends_on:
    - create_comment
  args:
    case_id: ${{ TRIGGER.case_id }}
    parent_comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Added IOC context."
- ref: update_comment
  action: core.cases.update_comment
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
    content: "Initial triage started. Waiting on endpoint data."
- ref: list_comments
  action: core.cases.list_comments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: list_comment_threads
  action: core.cases.list_comment_threads
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_comment_thread
  action: core.cases.get_comment_thread
  depends_on:
    - create_comment
  args:
    comment_id: ${{ ACTIONS.create_comment.result.id }}
```

## `core.cases.assign_user`

Assign a user to an existing case.

### Inputs

<ResponseField name="assignee_id" type="string">

Required.

The ID of the user to assign to the case.

</ResponseField>

<ResponseField name="case_id" type="string">

Required.

The ID of the case to assign a user to.

</ResponseField>

### Examples

**Assign a case**

```yaml
- ref: assign_by_id
  action: core.cases.assign_user
  args:
    case_id: ${{ TRIGGER.case_id }}
    assignee_id: ${{ TRIGGER.assignee_id }}
- ref: assign_by_email
  action: core.cases.assign_user_by_email
  args:
    case_id: ${{ TRIGGER.case_id }}
    assignee_email: analyst@example.com
```

## `core.cases.assign_user_by_email`

Assign a user to an existing case by email.

### Inputs

<ResponseField name="assignee_email" type="string">

Required.

The email of the user to assign to the case.

</ResponseField>

<ResponseField name="case_id" type="string">

Required.

The ID of the case to assign a user to.

</ResponseField>

### Examples

**Assign a case**

```yaml
- ref: assign_by_id
  action: core.cases.assign_user
  args:
    case_id: ${{ TRIGGER.case_id }}
    assignee_id: ${{ TRIGGER.assignee_id }}
- ref: assign_by_email
  action: core.cases.assign_user_by_email
  args:
    case_id: ${{ TRIGGER.case_id }}
    assignee_email: analyst@example.com
```

## `core.cases.add_case_tag`

Add a tag to a case by tag ID or ref.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to add a tag to.

</ResponseField>

<ResponseField name="tag" type="string">

Required.

The tag identifier (ID or ref) to add to the case.

</ResponseField>

<ResponseField name="create_if_missing" type="boolean">

Optional.

If true, create the tag if it does not exist.

Default: `false`.

</ResponseField>

### Examples

**Add and remove tags**

```yaml
- ref: add_tag
  action: core.cases.add_case_tag
  args:
    case_id: ${{ TRIGGER.case_id }}
    tag: credential-theft
    create_if_missing: true
- ref: remove_tag
  action: core.cases.remove_case_tag
  args:
    case_id: ${{ TRIGGER.case_id }}
    tag: needs-triage
```

## `core.cases.remove_case_tag`

Remove a tag from a case by tag ID or ref.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to remove a tag from.

</ResponseField>

<ResponseField name="tag" type="string">

Required.

The tag identifier (ID or ref) to remove from the case.

</ResponseField>

### Examples

**Add and remove tags**

```yaml
- ref: add_tag
  action: core.cases.add_case_tag
  args:
    case_id: ${{ TRIGGER.case_id }}
    tag: credential-theft
    create_if_missing: true
- ref: remove_tag
  action: core.cases.remove_case_tag
  args:
    case_id: ${{ TRIGGER.case_id }}
    tag: needs-triage
```

## `core.cases.upload_attachment`

Upload a file attachment to a case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to attach the file to.

</ResponseField>

<ResponseField name="content_base64" type="string">

Required.

The file content encoded in base64.

</ResponseField>

<ResponseField name="content_type" type="string">

Required.

The MIME type of the file (e.g., 'application/pdf').

</ResponseField>

<ResponseField name="file_name" type="string">

Required.

The original filename.

</ResponseField>

### Examples

**Work with attachments**

```yaml
- ref: upload_attachment
  action: core.cases.upload_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    file_name: note.txt
    content_base64: Zm9yZW5zaWMgbm90ZXM=
    content_type: text/plain
- ref: upload_attachment_from_url
  action: core.cases.upload_attachment_from_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    url: https://artifacts.example.com/evidence/${{ TRIGGER.alert_id }}
    headers:
      Authorization: Bearer ${{ SECRETS.artifacts.API_TOKEN }}
    file_name: evidence.json
- ref: list_attachments
  action: core.cases.list_attachments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_attachment
  action: core.cases.get_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: get_download_url
  action: core.cases.get_attachment_download_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
    expiry: 900
- ref: download_attachment
  action: core.cases.download_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: delete_attachment
  action: core.cases.delete_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
```

## `core.cases.upload_attachment_from_url`

Upload a file attachment to a case from a URL.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to attach the file to.

</ResponseField>

<ResponseField name="url" type="string">

Required.

The URL of the file to upload.

</ResponseField>

<ResponseField name="file_name" type="string | null">

Optional.

Filename of the file to upload. If not provided, the filename will be inferred from the URL.

Default: `null`.

</ResponseField>

<ResponseField name="headers" type="map[string, string] | null">

Optional.

The headers to use when downloading the file.

Default: `null`.

</ResponseField>

### Examples

**Work with attachments**

```yaml
- ref: upload_attachment
  action: core.cases.upload_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    file_name: note.txt
    content_base64: Zm9yZW5zaWMgbm90ZXM=
    content_type: text/plain
- ref: upload_attachment_from_url
  action: core.cases.upload_attachment_from_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    url: https://artifacts.example.com/evidence/${{ TRIGGER.alert_id }}
    headers:
      Authorization: Bearer ${{ SECRETS.artifacts.API_TOKEN }}
    file_name: evidence.json
- ref: list_attachments
  action: core.cases.list_attachments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_attachment
  action: core.cases.get_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: get_download_url
  action: core.cases.get_attachment_download_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
    expiry: 900
- ref: download_attachment
  action: core.cases.download_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: delete_attachment
  action: core.cases.delete_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
```

## `core.cases.list_attachments`

List all attachments for a case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to list attachments for.

</ResponseField>

### Examples

**Work with attachments**

```yaml
- ref: upload_attachment
  action: core.cases.upload_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    file_name: note.txt
    content_base64: Zm9yZW5zaWMgbm90ZXM=
    content_type: text/plain
- ref: upload_attachment_from_url
  action: core.cases.upload_attachment_from_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    url: https://artifacts.example.com/evidence/${{ TRIGGER.alert_id }}
    headers:
      Authorization: Bearer ${{ SECRETS.artifacts.API_TOKEN }}
    file_name: evidence.json
- ref: list_attachments
  action: core.cases.list_attachments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_attachment
  action: core.cases.get_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: get_download_url
  action: core.cases.get_attachment_download_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
    expiry: 900
- ref: download_attachment
  action: core.cases.download_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: delete_attachment
  action: core.cases.delete_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
```

## `core.cases.get_attachment`

Get attachment metadata without downloading the content.

### Inputs

<ResponseField name="attachment_id" type="string">

Required.

The ID of the attachment to get.

</ResponseField>

<ResponseField name="case_id" type="string">

Required.

The ID of the case containing the attachment.

</ResponseField>

### Examples

**Work with attachments**

```yaml
- ref: upload_attachment
  action: core.cases.upload_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    file_name: note.txt
    content_base64: Zm9yZW5zaWMgbm90ZXM=
    content_type: text/plain
- ref: upload_attachment_from_url
  action: core.cases.upload_attachment_from_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    url: https://artifacts.example.com/evidence/${{ TRIGGER.alert_id }}
    headers:
      Authorization: Bearer ${{ SECRETS.artifacts.API_TOKEN }}
    file_name: evidence.json
- ref: list_attachments
  action: core.cases.list_attachments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_attachment
  action: core.cases.get_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: get_download_url
  action: core.cases.get_attachment_download_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
    expiry: 900
- ref: download_attachment
  action: core.cases.download_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: delete_attachment
  action: core.cases.delete_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
```

## `core.cases.get_attachment_download_url`

Get a presigned S3 URL for downloading an attachment.

### Inputs

<ResponseField name="attachment_id" type="string">

Required.

The ID of the attachment.

</ResponseField>

<ResponseField name="case_id" type="string">

Required.

The ID of the case containing the attachment.

</ResponseField>

<ResponseField name="expiry" type="integer | null">

Optional.

URL expiry time in seconds. If not provided, uses the default from configuration.

Default: `null`.

</ResponseField>

### Examples

**Work with attachments**

```yaml
- ref: upload_attachment
  action: core.cases.upload_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    file_name: note.txt
    content_base64: Zm9yZW5zaWMgbm90ZXM=
    content_type: text/plain
- ref: upload_attachment_from_url
  action: core.cases.upload_attachment_from_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    url: https://artifacts.example.com/evidence/${{ TRIGGER.alert_id }}
    headers:
      Authorization: Bearer ${{ SECRETS.artifacts.API_TOKEN }}
    file_name: evidence.json
- ref: list_attachments
  action: core.cases.list_attachments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_attachment
  action: core.cases.get_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: get_download_url
  action: core.cases.get_attachment_download_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
    expiry: 900
- ref: download_attachment
  action: core.cases.download_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: delete_attachment
  action: core.cases.delete_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
```

## `core.cases.download_attachment`

Download an attachment's content.

### Inputs

<ResponseField name="attachment_id" type="string">

Required.

The ID of the attachment to download.

</ResponseField>

<ResponseField name="case_id" type="string">

Required.

The ID of the case containing the attachment.

</ResponseField>

### Examples

**Work with attachments**

```yaml
- ref: upload_attachment
  action: core.cases.upload_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    file_name: note.txt
    content_base64: Zm9yZW5zaWMgbm90ZXM=
    content_type: text/plain
- ref: upload_attachment_from_url
  action: core.cases.upload_attachment_from_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    url: https://artifacts.example.com/evidence/${{ TRIGGER.alert_id }}
    headers:
      Authorization: Bearer ${{ SECRETS.artifacts.API_TOKEN }}
    file_name: evidence.json
- ref: list_attachments
  action: core.cases.list_attachments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_attachment
  action: core.cases.get_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: get_download_url
  action: core.cases.get_attachment_download_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
    expiry: 900
- ref: download_attachment
  action: core.cases.download_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: delete_attachment
  action: core.cases.delete_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
```

## `core.cases.delete_attachment`

Delete an attachment from a case.

### Inputs

<ResponseField name="attachment_id" type="string">

Required.

The ID of the attachment to delete.

</ResponseField>

<ResponseField name="case_id" type="string">

Required.

The ID of the case containing the attachment.

</ResponseField>

### Examples

**Work with attachments**

```yaml
- ref: upload_attachment
  action: core.cases.upload_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    file_name: note.txt
    content_base64: Zm9yZW5zaWMgbm90ZXM=
    content_type: text/plain
- ref: upload_attachment_from_url
  action: core.cases.upload_attachment_from_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    url: https://artifacts.example.com/evidence/${{ TRIGGER.alert_id }}
    headers:
      Authorization: Bearer ${{ SECRETS.artifacts.API_TOKEN }}
    file_name: evidence.json
- ref: list_attachments
  action: core.cases.list_attachments
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: get_attachment
  action: core.cases.get_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: get_download_url
  action: core.cases.get_attachment_download_url
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
    expiry: 900
- ref: download_attachment
  action: core.cases.download_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
- ref: delete_attachment
  action: core.cases.delete_attachment
  args:
    case_id: ${{ TRIGGER.case_id }}
    attachment_id: ${{ TRIGGER.attachment_id }}
```

## `core.cases.link_row`

Link an existing table row to a case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

Case ID

</ResponseField>

<ResponseField name="row_id" type="string">

Required.

Row ID

</ResponseField>

<ResponseField name="table_id" type="string">

Required.

Table ID

</ResponseField>

### Examples

**Link table rows to a case**

```yaml
- ref: link_row
  action: core.cases.link_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row_id: ${{ TRIGGER.row_id }}
- ref: unlink_row
  action: core.cases.unlink_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row_id: ${{ TRIGGER.row_id }}
- ref: insert_case_row
  action: core.cases.insert_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row:
      value: ${{ TRIGGER.indicator }}
      indicator_type: domain
```

## `core.cases.unlink_row`

Remove a linked table row from a case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

Case ID

</ResponseField>

<ResponseField name="row_id" type="string">

Required.

Row ID

</ResponseField>

<ResponseField name="table_id" type="string">

Required.

Table ID

</ResponseField>

### Examples

**Link table rows to a case**

```yaml
- ref: link_row
  action: core.cases.link_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row_id: ${{ TRIGGER.row_id }}
- ref: unlink_row
  action: core.cases.unlink_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row_id: ${{ TRIGGER.row_id }}
- ref: insert_case_row
  action: core.cases.insert_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row:
      value: ${{ TRIGGER.indicator }}
      indicator_type: domain
```

## `core.cases.insert_row`

Insert a row into a table and link it to a case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

Case ID

</ResponseField>

<ResponseField name="row" type="object">

Required.

Row values

</ResponseField>

<ResponseField name="table_id" type="string">

Required.

Table ID

</ResponseField>

### Examples

**Link table rows to a case**

```yaml
- ref: link_row
  action: core.cases.link_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row_id: ${{ TRIGGER.row_id }}
- ref: unlink_row
  action: core.cases.unlink_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row_id: ${{ TRIGGER.row_id }}
- ref: insert_case_row
  action: core.cases.insert_row
  args:
    case_id: ${{ TRIGGER.case_id }}
    table_id: indicators
    row:
      value: ${{ TRIGGER.indicator }}
      indicator_type: domain
```

## `core.cases.create_task`

Create a new task for a case.

<Badge icon="lock" color="blue" size="lg" shape="pill">Enterprise Edition</Badge>

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to create a task for.

</ResponseField>

<ResponseField name="title" type="string">

Required.

The title of the task.

</ResponseField>

<ResponseField name="assignee_id" type="string | null">

Optional.

The ID of the user to assign the task to.

Default: `null`.

</ResponseField>

<ResponseField name="default_trigger_values" type="object | null">

Optional.

The default trigger values for the task.

Default: `null`.

</ResponseField>

<ResponseField name="description" type="string | null">

Optional.

The description of the task.

Default: `null`.

</ResponseField>

<ResponseField name="priority" type="string">

Optional.

The priority of the task (unknown, low, medium, high, critical).

Default: `"unknown"`.

</ResponseField>

<ResponseField name="status" type="string">

Optional.

The status of the task (todo, in_progress, blocked, completed).

Default: `"todo"`.

</ResponseField>

<ResponseField name="workflow_id" type="string | null">

Optional.

The ID of the workflow associated with this task.

Default: `null`.

</ResponseField>

### Examples

**Manage case tasks**

```yaml
- ref: create_task
  action: core.cases.create_task
  args:
    case_id: ${{ TRIGGER.case_id }}
    title: Collect email headers
    description: Pull the original message from the mail gateway.
    priority: high
    status: todo
- ref: get_task
  action: core.cases.get_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
- ref: list_tasks
  action: core.cases.list_tasks
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: update_task
  action: core.cases.update_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
    status: in_progress
- ref: delete_task
  action: core.cases.delete_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
```

## `core.cases.get_task`

Get a specific case task by ID.

<Badge icon="lock" color="blue" size="lg" shape="pill">Enterprise Edition</Badge>

### Inputs

<ResponseField name="task_id" type="string">

Required.

The ID of the task to retrieve.

</ResponseField>

### Examples

**Manage case tasks**

```yaml
- ref: create_task
  action: core.cases.create_task
  args:
    case_id: ${{ TRIGGER.case_id }}
    title: Collect email headers
    description: Pull the original message from the mail gateway.
    priority: high
    status: todo
- ref: get_task
  action: core.cases.get_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
- ref: list_tasks
  action: core.cases.list_tasks
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: update_task
  action: core.cases.update_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
    status: in_progress
- ref: delete_task
  action: core.cases.delete_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
```

## `core.cases.list_tasks`

List all tasks for a case.

<Badge icon="lock" color="blue" size="lg" shape="pill">Enterprise Edition</Badge>

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to list tasks for.

</ResponseField>

### Examples

**Manage case tasks**

```yaml
- ref: create_task
  action: core.cases.create_task
  args:
    case_id: ${{ TRIGGER.case_id }}
    title: Collect email headers
    description: Pull the original message from the mail gateway.
    priority: high
    status: todo
- ref: get_task
  action: core.cases.get_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
- ref: list_tasks
  action: core.cases.list_tasks
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: update_task
  action: core.cases.update_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
    status: in_progress
- ref: delete_task
  action: core.cases.delete_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
```

## `core.cases.update_task`

Update an existing case task.

<Badge icon="lock" color="blue" size="lg" shape="pill">Enterprise Edition</Badge>

### Inputs

<ResponseField name="task_id" type="string">

Required.

The ID of the task to update.

</ResponseField>

<ResponseField name="assignee_id" type="string | null">

Optional.

The ID of the user to assign the task to.

Default: `null`.

</ResponseField>

<ResponseField name="default_trigger_values" type="object | null">

Optional.

The default trigger values for the task.

Default: `null`.

</ResponseField>

<ResponseField name="description" type="string | null">

Optional.

The updated description of the task.

Default: `null`.

</ResponseField>

<ResponseField name="priority" type="string | null">

Optional.

The updated priority of the task (unknown, low, medium, high, critical).

Default: `null`.

</ResponseField>

<ResponseField name="status" type="string | null">

Optional.

The updated status of the task (todo, in_progress, blocked, completed).

Default: `null`.

</ResponseField>

<ResponseField name="title" type="string | null">

Optional.

The updated title of the task.

Default: `null`.

</ResponseField>

<ResponseField name="workflow_id" type="string | null">

Optional.

The ID of the workflow associated with this task.

Default: `null`.

</ResponseField>

### Examples

**Manage case tasks**

```yaml
- ref: create_task
  action: core.cases.create_task
  args:
    case_id: ${{ TRIGGER.case_id }}
    title: Collect email headers
    description: Pull the original message from the mail gateway.
    priority: high
    status: todo
- ref: get_task
  action: core.cases.get_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
- ref: list_tasks
  action: core.cases.list_tasks
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: update_task
  action: core.cases.update_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
    status: in_progress
- ref: delete_task
  action: core.cases.delete_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
```

## `core.cases.delete_task`

Delete a case task.

<Badge icon="lock" color="blue" size="lg" shape="pill">Enterprise Edition</Badge>

### Inputs

<ResponseField name="task_id" type="string">

Required.

The ID of the task to delete.

</ResponseField>

### Examples

**Manage case tasks**

```yaml
- ref: create_task
  action: core.cases.create_task
  args:
    case_id: ${{ TRIGGER.case_id }}
    title: Collect email headers
    description: Pull the original message from the mail gateway.
    priority: high
    status: todo
- ref: get_task
  action: core.cases.get_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
- ref: list_tasks
  action: core.cases.list_tasks
  args:
    case_id: ${{ TRIGGER.case_id }}
- ref: update_task
  action: core.cases.update_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
    status: in_progress
- ref: delete_task
  action: core.cases.delete_task
  depends_on:
    - create_task
  args:
    task_id: ${{ ACTIONS.create_task.result.id }}
```

## `core.cases.get_case_metrics`

Get case metrics as OTEL-aligned time-series for the provided case IDs.

<Badge icon="lock" color="blue" size="lg" shape="pill">Enterprise Edition</Badge>

### Inputs

<ResponseField name="case_ids" type="array[string]">

Required.

List of case IDs to get case metrics for.

</ResponseField>

### Examples

**Read case metrics**

```yaml
- ref: case_metrics
  action: core.cases.get_case_metrics
  args:
    case_ids:
      - ${{ TRIGGER.case_id }}
      - ${{ TRIGGER.related_case_id }}
```

## `core.cases.delete_case`

Delete a case.

### Inputs

<ResponseField name="case_id" type="string">

Required.

The ID of the case to delete.

</ResponseField>

### Examples

**Delete a case**

```yaml
- ref: close_case
  action: core.cases.update_case
  args:
    case_id: ${{ TRIGGER.case_id }}
    status: closed
- ref: delete_case
  action: core.cases.delete_case
  depends_on:
    - close_case
  args:
    case_id: ${{ TRIGGER.case_id }}
```
