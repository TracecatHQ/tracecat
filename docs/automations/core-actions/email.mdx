---
title: "Email"
---

{/* Auto-generated by scripts/generate_core_action_docs.py; do not edit by hand. */}

## `core.send_email_smtp`

Run a send email action.

### Inputs

<ResponseField name="body" type="string">

Required.

Body content of the email

</ResponseField>

<ResponseField name="recipients" type="array[string]">

Required.

List of recipient email addresses

</ResponseField>

<ResponseField name="sender" type="string">

Required.

Email address of the sender

</ResponseField>

<ResponseField name="subject" type="string">

Required.

Subject of the email

</ResponseField>

<ResponseField name="allowed_attributes" type="map[string, array[string]] | null">

Optional.

Key-value pairs of allowed attributes to add to the email body. e.g. &#123;'*': ['style']&#125;. This allows all tags to have a style attribute

Default: `null`.

</ResponseField>

<ResponseField name="bcc" type="string | array[string] | null">

Optional.

Recipient(s) to be copied in the BCC field

Default: `null`.

</ResponseField>

<ResponseField name="cc" type="string | array[string] | null">

Optional.

Recipient(s) to be copied in the CC field

Default: `null`.

</ResponseField>

<ResponseField name="content_type" type="string">

Optional.

Email content type ('text/plain' or 'text/html'). Defaults to 'text/plain'.

Default: `"text/plain"`.

Allowed values: `text/plain`, `text/html`.

</ResponseField>

<ResponseField name="enable_auth" type="boolean">

Optional.

Enable SMTP authentication

Default: `false`.

</ResponseField>

<ResponseField name="enable_ssl" type="boolean">

Optional.

Enable SSL for secure connection

Default: `false`.

</ResponseField>

<ResponseField name="enable_starttls" type="boolean">

Optional.

Enable STARTTLS for secure connection

Default: `false`.

</ResponseField>

<ResponseField name="headers" type="map[string, string] | null">

Optional.

Additional email headers

Default: `null`.

</ResponseField>

<ResponseField name="ignore_cert_errors" type="boolean">

Optional.

Ignore SSL certificate errors

Default: `false`.

</ResponseField>

<ResponseField name="reply_to" type="string | array[string] | null">

Optional.

Email address(es) to be used in the Reply-To field

Default: `null`.

</ResponseField>

<ResponseField name="sender_prefix" type="string | null">

Optional.

Email address prefix of the sender

Default: `null`.

</ResponseField>

<ResponseField name="timeout" type="number | null">

Optional.

Timeout for SMTP operations in seconds

Default: `null`.

</ResponseField>

### Examples

**Send a notification**

```yaml
- ref: notify_team
  action: core.send_email_smtp
  args:
    sender: no-reply@example.com
    recipients:
      - secops@example.com
    subject: "Tracecat alert ${{ TRIGGER.alert_id }}"
    body: "Severity: ${{ TRIGGER.severity }}"
    content_type: text/plain
    enable_starttls: true
    enable_auth: true
```
