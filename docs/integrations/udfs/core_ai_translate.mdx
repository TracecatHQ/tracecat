---
title: AI Translate
description: core.ai.translate
---

Perform translation using a LLM.

This is the JSONSchema7 definition for the core.ai.translate integration.

_No secrets required._

## Inputs

<CodeGroup>
```json JSONSchema7 Definition
{
    "additionalProperties": false,
    "properties": {
        "execution_context": {
            "default": null,
            "description": "The context of the execution",
            "title": "Execution Context",
            "type": "object"
        },
        "from_language": {
            "default": null,
            "description": "The source language for the translation. If None, the language(s) will be detected automatically.",
            "title": "From Language",
            "type": "string"
        },
        "response_schema": {
            "additionalProperties": {
                "type": "string"
            },
            "default": null,
            "description": "Shape of the response",
            "title": "Response Schema",
            "type": "object"
        },
        "text": {
            "description": "Text passed to the AI",
            "title": "Text",
            "type": "string"
        },
        "to_language": {
            "default": "english",
            "description": "The target language for translation",
            "title": "To Language",
            "type": "string"
        }
    },
    "required": [
        "text"
    ],
    "title": "CoreAiTranslate",
    "type": "object"
}
```

</CodeGroup>

## Response

<CodeGroup>
```json JSONSchema7 Definition
{
    "properties": {
        "data": {
            "anyOf": [
                {
                    "type": "string"
                },
                {
                    "type": "object"
                }
            ],
            "title": "Data"
        }
    },
    "required": [
        "data"
    ],
    "title": "LLMResponse",
    "type": "object"
}
```

</CodeGroup>
