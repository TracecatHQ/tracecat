{/* Auto-generated by scripts/generate_env_docs.py; do not edit by hand. */}

## General

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `LOG_LEVEL` | `INFO` |  |
| `COMPOSE_PROJECT_NAME` | `tracecat` |  |
| `COMPOSE_BAKE` | `true` |  |

## Network configuration

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `PUBLIC_APP_PORT` | `80` |  |
| `PUBLIC_APP_URL` | `http://localhost:${PUBLIC_APP_PORT}` |  |
| `PUBLIC_API_URL` | `${PUBLIC_APP_URL}/api` |  |
| `INTERNAL_API_URL` | `http://api:8000` |  |

## Caddy

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `BASE_DOMAIN` | `:${PUBLIC_APP_PORT}` |  |
| `ADDRESS` | `0.0.0.0` |  |

## Frontend

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `NODE_ENV` | `development` |  |
| `NEXT_PUBLIC_APP_ENV` | `development` |  |
| `NEXT_PUBLIC_APP_URL` | `${PUBLIC_APP_URL}` | The frontend app URL |
| `NEXT_PUBLIC_API_URL` | `${PUBLIC_API_URL}` | Allows the browser to communicate with the backend |
| `NEXT_SERVER_API_URL` | `${INTERNAL_API_URL}` | Allows the frontend server (inside docker) to communicate with the backend server (inside docker) |

## Application

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__APP_ENV` | `development` | One of `development`, `staging`, or `production` |
| `TRACECAT__DB_ENCRYPTION_KEY` | `your-tracecat-db-fernet-key` | Used to encrypt/decrypt sensitive keys in the database Can be generated using `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"` |
| `TRACECAT__SERVICE_KEY` | `your-tracecat-service-key` | Used to authenticate with Tracecat services Can be generated using `openssl rand -hex 32` |
| `TRACECAT__EXECUTOR_TOKEN_TTL_SECONDS` | `900` |  |
| `TRACECAT__SIGNING_SECRET` | `your-tracecat-signing-secret` | Used to generate private webhook URLs Can be generated using `openssl rand -hex 32` |
| `TRACECAT__API_URL` | `${INTERNAL_API_URL}` | API Service URL |
| `TRACECAT__API_ROOT_PATH` | `/api` | Root path to deal with extra path prefix behind the reverse proxy |
| `TRACECAT__PUBLIC_APP_URL` | `${PUBLIC_APP_URL}` | This the public URL for the frontend |
| `TRACECAT__PUBLIC_API_URL` | `${PUBLIC_API_URL}` | This is the public URL for incoming webhooks If you wish to expose your webhooks to the internet, you can use a tunneling service like ngrok. If using ngrok, run `ngrok http --domain=INSERT_STATIC_NGROK_DOMAIN_HERE 8001` to start ngrok and update this with the forwarding URL |
| `TRACECAT__ALLOW_ORIGINS` | `http://localhost:3000,${PUBLIC_APP_URL}` | CORS (comma-separated string of allowed origins) |
| `TRACECAT__DB_SSLMODE` | `disable` | Postgres SSL mode |

## Postgres

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__POSTGRES_USER` | `postgres` |  |
| `TRACECAT__POSTGRES_PASSWORD` | `postgres` |  |
| `TRACECAT__DB_URI` | `postgresql+psycopg://${TRACECAT__POSTGRES_USER}:${TRACECAT__POSTGRES_PASSWORD}@postgres_db:5432/postgres` |  |

## Authentication

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__AUTH_SUPERADMIN_EMAIL` |  | First user to promote to superadmin |
| `TRACECAT__AUTH_TYPES` | `basic` | One or more comma-separated values from `basic`, `oidc`, `saml` |
| `TRACECAT__AUTH_ALLOWED_DOMAINS` |  | One or more comma-separated domains, e.g. `example.com,example.org` Leave blank to allow all domains |
| `TRACECAT__AUTH_MIN_PASSWORD_LENGTH` | `12` |  |
| `OIDC_ISSUER` |  |  |
| `OIDC_CLIENT_ID` |  |  |
| `OIDC_CLIENT_SECRET` |  |  |
| `OIDC_SCOPES` | `openid profile email` |  |
| `TRACECAT_MCP__FILE_TRANSFER_URL_EXPIRY_SECONDS` | `300` |  |
| `TRACECAT_MCP__STARTUP_MAX_ATTEMPTS` | `3` |  |
| `TRACECAT_MCP__STARTUP_RETRY_DELAY_SECONDS` | `2` |  |
| `OAUTH_CLIENT_ID` |  |  |
| `OAUTH_CLIENT_SECRET` |  |  |
| `USER_AUTH_SECRET` | `your-auth-secret` | Used for password reset and email verification tokens Can be generated using `openssl rand -hex 32` |
| `SAML_IDP_METADATA_URL` |  |  |
| `SAML_ACCEPTED_TIME_DIFF` | `3` |  |

## Temporal

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TEMPORAL__CLUSTER_URL` | `temporal:7233` |  |
| `TEMPORAL__CLUSTER_QUEUE` | `tracecat-task-queue` |  |
| `TEMPORAL__CLUSTER_NAMESPACE` | `default` |  |
| `TEMPORAL__POSTGRES_USER` | `temporal` |  |
| `TEMPORAL__POSTGRES_PASSWORD` | `temporal` |  |
| `TEMPORAL__UI_VERSION` | `latest` |  |
| `TEMPORAL__API_KEY` |  | Leave empty if not using Temporal Cloud |

## Executor

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__EXECUTOR_BACKEND` | `direct` |  |
| `TRACECAT__DISABLE_NSJAIL` | `true` |  |

## Workflow Artifacts

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__WORKFLOW_ARTIFACT_RETENTION_DAYS` | `30` | Retention period in days for workflow artifacts in blob storage. Objects older than this will be automatically deleted via S3 lifecycle rules. Set to 0 to disable automatic expiration. Default: 30 days (matches Temporal Cloud workflow history retention). |

## Blob storage

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__BLOB_STORAGE_BUCKET_WORKFLOW` | `tracecat-workflow` |  |
| `TRACECAT__BLOB_STORAGE_BUCKET_ATTACHMENTS` | `tracecat-attachments` |  |
| `TRACECAT__BLOB_STORAGE_BUCKET_REGISTRY` | `tracecat-registry` |  |

## MinIO

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `MINIO_ROOT_USER` | `minio` |  |
| `MINIO_ROOT_PASSWORD` | `password` |  |

## Redis

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `REDIS_HOST` | `redis` |  |
| `REDIS_PORT` | `6379` |  |
| `REDIS_URL` | `redis://${REDIS_HOST}:${REDIS_PORT}` |  |

## Externalization

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__RESULT_EXTERNALIZATION_ENABLED` | `true` | Store large action results and triggers in blob storage instead of Temporal history. |
| `TRACECAT__COLLECTION_MANIFESTS_ENABLED` | `true` | Store large collections as chunked manifests in blob storage. |
| `TRACECAT__RESULT_EXTERNALIZATION_THRESHOLD_BYTES` | `128000` | Threshold in bytes above which payloads are externalized to blob storage. |

## Local registry

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__LOCAL_REPOSITORY_ENABLED` | `false` | Enable this only for local-registry development. Leave disabled for normal remote/builtin registry use. |
| `TRACECAT__LOCAL_REPOSITORY_PATH` | `/absolute/path/to/internal-registry` | Absolute host path to the local registry package for Docker bind mounts. |
| `PYTHONPATH` | `/home/apiuser/.local:/app/local_registry` | Keep the default unless you need extra import paths in the executor containers. |

## Enterprise

| Variable | Default | Description |
| :------- | :------ | :---------- |
| `TRACECAT__FEATURE_FLAGS` |  | Feature flags (comma-separated list) |
| `TRACECAT__EE_MULTI_TENANT` | `false` |  |
