Core actions are Tracecat's built-in primitives for requests, transforms, workflow control, cases, and tables.
Each third-party integration under `tools.*` is a YAML template built from these core action steps, or a Python UDF.

See [Core actions](/automations/core-actions/request-actions/http) for every core action and its inputs, and [Integrations](/tools/slack) for the full catalog.
