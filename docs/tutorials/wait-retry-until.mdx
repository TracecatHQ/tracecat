---
title: Wait and retries
description: Define wait until and retry until logic in workflows.
---

## What you'll learn

- How wait until some time (e.g. "tomorrow 2pm") before running an action.
- How to use the `FN.is_working_hours` inline function.
- How to retry a child workflow until a condition is met.

## Tutorial

In this tutorial, we'll build a workflow that receives an external alert via webhook,
looks


### Prerequisites

- A Slack bot token and channel to send messages to.
- Slack token has permissions to write and read messages in the channel.

### Steps
