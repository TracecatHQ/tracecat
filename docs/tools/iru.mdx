---
title: "Iru (Kandji)"
description: "Reference for the Tracecat Iru (Kandji) integration: registered actions, required secrets, expected inputs, and example workflow usage."
---

{/* Auto-generated by scripts/generate_tool_docs.py; do not edit by hand. */}
## Add device note

Action ID: `tools.iru.add_device_note`

Adds a note to a device record.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="note" type="string" required>

Note content to add.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## Delete device

Action ID: `tools.iru.delete_device`

Deletes the device record, unenrolls the device from MDM, and causes the Iru agent to uninstall itself at next check-in. This cannot be undone.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## Erase device

Action ID: `tools.iru.erase_device`

Irreversibly erases a device and optionally controls platform-specific erase behavior.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="disallow_proximity_setup" type="boolean | null">

Whether to disallow proximity setup after erase.

Default: `null`.

</ParamField>

<ParamField path="pin" type="string | null">

Six-character macOS erase PIN; ignored for iOS, iPadOS, and tvOS.

Default: `null`.

</ParamField>

<ParamField path="preserve_data_plan" type="boolean | null">

Whether to preserve the cellular data plan during erase.

Default: `null`.

</ParamField>

## Get Activation Lock bypass code

Action ID: `tools.iru.get_activation_lock_bypass_code`

Gets a device's Activation Lock bypass code. Retrieval is recorded in the Iru audit log, and the code should be escrowed before any destructive offboarding step.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## Get device

Action ID: `tools.iru.get_device`

Gets a device record by UUID.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## Get device details

Action ID: `tools.iru.get_device_details`

Gets detailed inventory information for a device.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## Get FileVault recovery key

Action ID: `tools.iru.get_filevault_key`

Gets a device's FileVault recovery key. Retrieval is recorded in the Iru audit log, and the key should be escrowed before any destructive offboarding step.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## Get recovery lock password

Action ID: `tools.iru.get_recovery_password`

Gets a device's recovery lock password. Retrieval is recorded in the Iru audit log, and the password should be escrowed before any destructive offboarding step.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## List audit events

Action ID: `tools.iru.list_audit_events`

Lists cursor-paginated audit events, including Blueprint and Library Item changes, access to sensitive data such as FileVault and recovery keys, device lifecycle events, and API token management. Responses include next and previous URLs. This list-heavy endpoint shares the tenant-wide limit of 10,000 API requests per hour across all API tokens.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="cursor" type="string | null">

Cursor identifying the requested page.

Default: `null`.

</ParamField>

<ParamField path="end_date" type="string | null">

Latest event date to include.

Default: `null`.

</ParamField>

<ParamField path="limit" type="integer | null">

Results per page. Maximum 500.

Default: `null`.

</ParamField>

<ParamField path="sort_by" type="string | null">

Sort field, occurred_at or id; prefix with - for descending order.

Default: `null`.

</ParamField>

<ParamField path="start_date" type="string | null">

Earliest event date to include.

Default: `null`.

</ParamField>

## List behavioral detections

Action ID: `tools.iru.list_behavioral_detections`

Lists EDR behavioral detections. Threat state is read-only through the API; there is no endpoint to release a detection from quarantine, and released is only a filter value.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="classification" type="string | null">

Detection classification to filter by.

Default: `null`.

</ParamField>

<ParamField path="date_range" type="integer | null">

Number of days to include.

Default: `null`.

</ParamField>

<ParamField path="detection_date_from" type="string | null">

Earliest detection date to include.

Default: `null`.

</ParamField>

<ParamField path="detection_date_to" type="string | null">

Latest detection date to include.

Default: `null`.

</ParamField>

<ParamField path="device_id" type="string | null">

Device UUID to filter by.

Default: `null`.

</ParamField>

<ParamField path="informational_tags" type="string | null">

Informational tags to filter by.

Default: `null`.

</ParamField>

<ParamField path="limit" type="integer | null">

Results per page. Maximum 1000.

Default: `null`.

</ParamField>

<ParamField path="malware_family" type="string | null">

Malware family to filter by.

Default: `null`.

</ParamField>

<ParamField path="offset" type="integer | null">

Number of results to skip.

Default: `null`.

</ParamField>

<ParamField path="parent_process_name" type="string | null">

Parent process name to filter by.

Default: `null`.

</ParamField>

<ParamField path="sort_by" type="string | null">

Field to sort results by.

Default: `null`.

</ParamField>

<ParamField path="status" type="string | null">

Detection status to filter by.

Default: `null`.

</ParamField>

<ParamField path="target_process_name" type="string | null">

Target process name to filter by.

Default: `null`.

</ParamField>

<ParamField path="term" type="string | null">

Free-text search term.

Default: `null`.

</ParamField>

<ParamField path="threat_id" type="string | null">

Threat UUID to filter by.

Default: `null`.

</ParamField>

## List blueprints

Action ID: `tools.iru.list_blueprints`

Lists tenant Blueprints with optional filtering and pagination.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="id" type="string | null">

Blueprint UUID to filter by.

Default: `null`.

</ParamField>

<ParamField path="id__in" type="string | null">

Comma-separated Blueprint UUIDs to include.

Default: `null`.

</ParamField>

<ParamField path="limit" type="integer | null">

Results per page. Maximum 300.

Default: `null`.

</ParamField>

<ParamField path="name" type="string | null">

Blueprint name substring to match.

Default: `null`.

</ParamField>

<ParamField path="offset" type="integer | null">

Number of results to skip.

Default: `null`.

</ParamField>

## List device activity

Action ID: `tools.iru.list_device_activity`

Lists activity records for a device.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="limit" type="integer | null">

Results per page. Maximum 300.

Default: `null`.

</ParamField>

<ParamField path="offset" type="integer | null">

Number of results to skip.

Default: `null`.

</ParamField>

## List device apps

Action ID: `tools.iru.list_device_apps`

Lists applications installed on a device.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## List device commands

Action ID: `tools.iru.list_device_commands`

Lists commands for a device. Status codes are 1 Pending, 2 Running, 3 Completed, 4 Failed, and 5 Not Now.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="limit" type="integer | null">

Results per page. Maximum 300.

Default: `null`.

</ParamField>

<ParamField path="offset" type="integer | null">

Number of results to skip.

Default: `null`.

</ParamField>

## List device library items

Action ID: `tools.iru.list_device_library_items`

Lists Library Items assigned to a device.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## List device parameters

Action ID: `tools.iru.list_device_parameters`

Lists management parameters for a device.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## List devices

Action ID: `tools.iru.list_devices`

Lists tenant devices as a polymorphic bare JSON array. This list-heavy endpoint shares the tenant-wide limit of 10,000 API requests per hour across all API tokens.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="asset_tag" type="string | null">

Asset tag to filter by.

Default: `null`.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="blueprint_id" type="string | null">

Blueprint UUID to filter by.

Default: `null`.

</ParamField>

<ParamField path="device_id" type="string | null">

Device UUID to filter by.

Default: `null`.

</ParamField>

<ParamField path="device_name" type="string | null">

Device name to filter by.

Default: `null`.

</ParamField>

<ParamField path="filevault_enabled" type="boolean | null">

Whether to filter for devices with FileVault enabled or disabled.

Default: `null`.

</ParamField>

<ParamField path="limit" type="integer | null">

Results per page. Maximum 300.

Default: `null`.

</ParamField>

<ParamField path="mac_address" type="string | null">

MAC address to filter by.

Default: `null`.

</ParamField>

<ParamField path="model" type="string | null">

Device model to filter by.

Default: `null`.

</ParamField>

<ParamField path="offset" type="integer | null">

Number of results to skip.

Default: `null`.

</ParamField>

<ParamField path="ordering" type="string | null">

Field to order results by; prefix with - to reverse the order.

Default: `null`.

</ParamField>

<ParamField path="os_version" type="string | null">

Operating system version to filter by.

Default: `null`.

</ParamField>

<ParamField path="platform" type="string | null">

Platform to filter by, such as Mac, iPad, iPhone, AppleTV, or Android.

Default: `null`.

</ParamField>

<ParamField path="serial_number" type="string | null">

Serial number to filter by.

Default: `null`.

</ParamField>

<ParamField path="tag_id" type="string | null">

Tag UUID to filter by.

Default: `null`.

</ParamField>

<ParamField path="tag_name" type="string | null">

Tag name to filter by.

Default: `null`.

</ParamField>

<ParamField path="user" type="string | null">

Assigned user to filter by.

Default: `null`.

</ParamField>

<ParamField path="user_email" type="string | null">

Assigned user email to filter by.

Default: `null`.

</ParamField>

<ParamField path="user_id" type="string | null">

Assigned user UUID to filter by.

Default: `null`.

</ParamField>

<ParamField path="user_name" type="string | null">

Assigned user name to filter by.

Default: `null`.

</ParamField>

## List threat details

Action ID: `tools.iru.list_threat_details`

Lists malware and potentially unwanted program detections. Threat state is read-only through the API; there is no endpoint to release a detection from quarantine, and released is only a filter value.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="classification" type="string | null">

Detection classification, such as malware or pup.

Default: `null`.

</ParamField>

<ParamField path="date_range" type="integer | null">

Number of days to include.

Default: `null`.

</ParamField>

<ParamField path="device_id" type="string | null">

Device UUID to filter by.

Default: `null`.

</ParamField>

<ParamField path="limit" type="integer | null">

Results per page. Maximum 1000.

Default: `null`.

</ParamField>

<ParamField path="offset" type="integer | null">

Number of results to skip.

Default: `null`.

</ParamField>

<ParamField path="sort_by" type="string | null">

Field to sort results by.

Default: `null`.

</ParamField>

<ParamField path="status" type="string | null">

Detection status, such as quarantined, not_quarantined, or released.

Default: `null`.

</ParamField>

<ParamField path="term" type="string | null">

Free-text search across device name and file hash.

Default: `null`.

</ParamField>

## Lock device

Action ID: `tools.iru.lock_device`

Locks a device. For macOS, an unlock PIN is generated and returned in the response. On Apple silicon running macOS earlier than 11.5, this deactivates the Mac and reactivation requires network access and a Secure Token administrator.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="message" type="string | null">

Message to display on the locked device.

Default: `null`.

</ParamField>

<ParamField path="phone_number" type="string | null">

Phone number to display on the locked device.

Default: `null`.

</ParamField>

## Query Prism inventory

Action ID: `tools.iru.query_prism`

Queries a fleet-wide Prism inventory category. This list-heavy endpoint shares the tenant-wide limit of 10,000 API requests per hour across all API tokens.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="category" type="string" required>

Prism category, such as activation_lock, application_firewall, apps, cellular, certificates, desktop_and_screensaver, device_information, filevault, gatekeeper_and_xprotect, installed_profiles, kernel_extensions, launch_agents_and_daemons, local_users, startup_settings, system_extensions, or transparency_database.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="blueprint_ids" type="array[string] | null">

Blueprint UUIDs to filter by.

Default: `null`.

</ParamField>

<ParamField path="device_families" type="array[string] | null">

Device families to filter by.

Default: `null`.

</ParamField>

<ParamField path="filter" type="object | null">

Provider-native Prism filter object.

Default: `null`.

</ParamField>

<ParamField path="limit" type="integer | null">

Results per page. Maximum 300.

Default: `null`.

</ParamField>

<ParamField path="offset" type="integer | null">

Number of results to skip.

Default: `null`.

</ParamField>

<ParamField path="sort_by" type="string | null">

Field to sort by; prefix with - to reverse the order.

Default: `null`.

</ParamField>

## Send device action

Action ID: `tools.iru.send_device_action`

Sends a payload-free device action. Lock and erase use dedicated actions because they accept request bodies.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="action" type="string" required>

Action to send, such as restart, shutdown, blankpush, dailycheckin, updateinventory, reinstallagent, renewmdmprofile, clearpasscode, enablelostmode, disablelostmode, playlostmodesound, updatelocation, or remotedesktop.

</ParamField>

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

## Update device

Action ID: `tools.iru.update_device`

Updates device assignment and metadata; there is no separate blueprint-assignment endpoint, so use this action to assign a blueprint during onboarding.

Reference: [https://api-docs.iru.com/](https://api-docs.iru.com/)

### Secrets

Required secrets:

- `iru`: required values `IRU_API_TOKEN`.

### Input fields

<ParamField path="device_id" type="string" required>

Device UUID.

</ParamField>

<ParamField path="asset_tag" type="string | null">

Asset tag to set on the device.

Default: `null`.

</ParamField>

<ParamField path="base_url" type="string | null">

Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).

Default: `null`.

</ParamField>

<ParamField path="blueprint_id" type="string | null">

Blueprint UUID to assign to the device.

Default: `null`.

</ParamField>

<ParamField path="tags" type="array[string] | null">

Tag UUIDs to assign to the device.

Default: `null`.

</ParamField>

<ParamField path="user" type="string | null">

User UUID to assign to the device.

Default: `null`.

</ParamField>
