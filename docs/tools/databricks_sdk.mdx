---
title: "Databricks SDK"
description: "Reference for the Tracecat Databricks SDK integration: registered actions, required secrets, expected inputs, and example workflow usage."
---

{/* Auto-generated by scripts/generate_tool_docs.py; do not edit by hand. */}
## Call method

Action ID: `tools.databricks_sdk.call_method`

Instantiate a Databricks workspace client and call an SDK method.

Reference: [https://databricks-sdk-py.readthedocs.io/en/latest/workspace/](https://databricks-sdk-py.readthedocs.io/en/latest/workspace/)

### Secrets

Required secrets:

- `databricks`: required values `DATABRICKS_HOST`; optional values `DATABRICKS_CLIENT_ID`, `DATABRICKS_CLIENT_SECRET`.

Optional secrets:

- `databricks_oauth`: OAuth token `DATABRICKS_USER_TOKEN`.
- `databricks_oauth`: OAuth token `DATABRICKS_SERVICE_TOKEN`.

### Input fields

<ParamField path="method_name" type="string" required>

Public Databricks SDK method name.

</ParamField>

<ParamField path="service" type="string" required>

WorkspaceClient service name, for example `clusters`, `jobs`, `warehouses`, or `statement_execution`.

</ParamField>

<ParamField path="params" type="object | null">

Databricks SDK method parameters. Values, including nulls, are forwarded unchanged.

Default: `null`.

</ParamField>

## Call paginated method

Action ID: `tools.databricks_sdk.call_paginated_method`

Instantiate a Databricks workspace client and collect a bounded number of items from an SDK iterator method.

Reference: [https://github.com/databricks/databricks-sdk-py#paginated-responses](https://github.com/databricks/databricks-sdk-py#paginated-responses)

### Secrets

Required secrets:

- `databricks`: required values `DATABRICKS_HOST`; optional values `DATABRICKS_CLIENT_ID`, `DATABRICKS_CLIENT_SECRET`.

Optional secrets:

- `databricks_oauth`: OAuth token `DATABRICKS_USER_TOKEN`.
- `databricks_oauth`: OAuth token `DATABRICKS_SERVICE_TOKEN`.

### Input fields

<ParamField path="method_name" type="string" required>

Public Databricks SDK iterator method name.

</ParamField>

<ParamField path="service" type="string" required>

WorkspaceClient service name.

</ParamField>

<ParamField path="limit" type="integer">

Maximum number of items to return across SDK pages.

Default: `1000`.

</ParamField>

<ParamField path="params" type="object | null">

Databricks SDK method parameters. Values, including nulls, are forwarded unchanged.

Default: `null`.

</ParamField>
