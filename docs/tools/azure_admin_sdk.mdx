---
title: "Azure Admin SDK"
description: "Reference for the Tracecat Azure Admin SDK integration: registered actions, required secrets, expected inputs, and example workflow usage."
---

{/* Auto-generated by scripts/generate_tool_docs.py; do not edit by hand. */}
## Call method

Action ID: `tools.azure_admin_sdk.call_method`

Instantiate an Azure Management SDK client and call a method.

Reference: [https://learn.microsoft.com/en-us/python/api/overview/azure/mgmt](https://learn.microsoft.com/en-us/python/api/overview/azure/mgmt)

### Secrets

Required secrets:

- `azure_management_oauth`: OAuth token `AZURE_MANAGEMENT_SERVICE_TOKEN`.

### Input fields

<ParamField path="client_class" type="string" required>

Azure management SDK client class name.

</ParamField>

<ParamField path="method_name" type="string" required>

Method name to call on the client or method path.

</ParamField>

<ParamField path="module_name" type="string" required>

Azure management SDK module, e.g. `azure.mgmt.resource`.

</ParamField>

<ParamField path="subscription_id" type="string" required>

Azure subscription ID for the management client.

</ParamField>

<ParamField path="client_kwargs" type="object | null">

Extra keyword arguments for the Azure SDK client. Credential and endpoint overrides (e.g. `base_url`, `endpoint`) are not allowed.

Default: `null`.

</ParamField>

<ParamField path="method_path" type="string | null">

Optional dotted path on the client, e.g. `resource_groups`.

Default: `null`.

</ParamField>

<ParamField path="params" type="object | null">

Parameters for the Azure SDK method.

Default: `null`.

</ParamField>

## Call paginated method

Action ID: `tools.azure_admin_sdk.call_paginated_method`

Instantiate an Azure Management SDK client and call a paginated method.

Reference: [https://learn.microsoft.com/en-us/python/api/overview/azure/mgmt](https://learn.microsoft.com/en-us/python/api/overview/azure/mgmt)

### Secrets

Required secrets:

- `azure_management_oauth`: OAuth token `AZURE_MANAGEMENT_SERVICE_TOKEN`.

### Input fields

<ParamField path="client_class" type="string" required>

Azure management SDK client class name.

</ParamField>

<ParamField path="method_name" type="string" required>

Paginated method name to call on the client or method path.

</ParamField>

<ParamField path="module_name" type="string" required>

Azure management SDK module, e.g. `azure.mgmt.resource`.

</ParamField>

<ParamField path="subscription_id" type="string" required>

Azure subscription ID for the management client.

</ParamField>

<ParamField path="client_kwargs" type="object | null">

Extra keyword arguments for the Azure SDK client. Credential and endpoint overrides (e.g. `base_url`, `endpoint`) are not allowed.

Default: `null`.

</ParamField>

<ParamField path="method_path" type="string | null">

Optional dotted path on the client, e.g. `resource_groups`.

Default: `null`.

</ParamField>

<ParamField path="params" type="object | null">

Parameters for the Azure SDK method.

Default: `null`.

</ParamField>
