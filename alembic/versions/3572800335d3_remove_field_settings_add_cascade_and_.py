"""remove field_settings add cascade and enum_options

Revision ID: 3572800335d3
Revises: 4701058ce39a
Create Date: 2025-08-12 15:19:41.810566

"""

from collections.abc import Sequence

import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

from alembic import op

# revision identifiers, used by Alembic.
revision: str = "3572800335d3"
down_revision: str | None = "4701058ce39a"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    # Drop indexes that are no longer needed
    op.drop_index(
        "idx_entity_data_null_fields",
        table_name="entity_data",
        postgresql_ops={"field_data": "jsonb_path_ops"},
        postgresql_using="gin",
        postgresql_where="(owner_id IS NOT NULL)",
    )
    op.drop_index(
        "idx_entity_data_unique_fields",
        table_name="entity_data",
        postgresql_using="gin",
        postgresql_where="(owner_id IS NOT NULL)",
    )

    # Add new columns with proper defaults
    op.add_column(
        "field_metadata",
        sa.Column(
            "relation_cascade_delete",
            sa.Boolean(),
            nullable=False,
            server_default="true",
        ),
    )
    op.add_column(
        "field_metadata",
        sa.Column(
            "enum_options", postgresql.JSONB(astext_type=sa.Text()), nullable=True
        ),
    )

    # Remove the old field_settings column
    op.drop_column("field_metadata", "field_settings")
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "field_metadata",
        sa.Column(
            "field_settings",
            postgresql.JSONB(astext_type=sa.Text()),
            autoincrement=False,
            nullable=True,
        ),
    )
    op.drop_column("field_metadata", "enum_options")
    op.drop_column("field_metadata", "relation_cascade_delete")
    op.create_index(
        "idx_entity_data_unique_fields",
        "entity_data",
        ["field_data"],
        unique=False,
        postgresql_using="gin",
        postgresql_where="(owner_id IS NOT NULL)",
    )
    op.create_index(
        "idx_entity_data_null_fields",
        "entity_data",
        ["field_data"],
        unique=False,
        postgresql_ops={"field_data": "jsonb_path_ops"},
        postgresql_using="gin",
        postgresql_where="(owner_id IS NOT NULL)",
    )
    # ### end Alembic commands ###
