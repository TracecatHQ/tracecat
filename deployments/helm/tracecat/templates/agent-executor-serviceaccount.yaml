{{- if .Values.agentExecutor.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tracecat.agentExecutorServiceAccountName" . }}
  labels:
    {{- include "tracecat.labels" . | nindent 4 }}
  {{- with .Values.agentExecutor.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
