{{- if .Values.agentExecutor.serviceAccount.create }}
{{- $annotations := mergeOverwrite (dict) .Values.serviceAccount.annotations .Values.agentExecutor.serviceAccount.annotations }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tracecat.agentExecutorServiceAccountName" . }}
  labels:
    {{- include "tracecat.labels" . | nindent 4 }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
