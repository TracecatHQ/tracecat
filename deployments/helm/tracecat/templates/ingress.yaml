{{- if .Values.ingress.enabled -}}
{{- $baseAnnotations := .Values.ingress.annotations | default dict -}}
{{- if .Values.ingress.split }}
{{- $uiAnnotations := merge (dict) $baseAnnotations (.Values.ingress.ui.annotations | default dict) -}}
{{- $apiAnnotations := merge (dict) $baseAnnotations (.Values.ingress.api.annotations | default dict) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tracecat.fullname" . }}-ui
  labels:
    {{- include "tracecat.labels" . | nindent 4 }}
  {{- with $uiAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          # UI (default/catch-all)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-ui
                port:
                  number: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tracecat.fullname" . }}-api
  labels:
    {{- include "tracecat.labels" . | nindent 4 }}
  {{- with $apiAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          # API service
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-api
                port:
                  number: 8000
{{- if .Values.mcp.enabled }}
---
{{- $mcpAnnotations := merge (dict) $baseAnnotations (.Values.ingress.mcp.annotations | default dict) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tracecat.fullname" . }}-mcp
  labels:
    {{- include "tracecat.labels" . | nindent 4 }}
  {{- with $mcpAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          # MCP server
          - path: /mcp
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          # MCP OIDC auth routes
          - path: /.well-known/oauth-authorization-server
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /.well-known/oauth-protected-resource
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /authorize
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /token
            pathType: Exact
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /register
            pathType: Exact
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /consent
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /auth/callback
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
{{- end }}
{{- else }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tracecat.fullname" . }}
  labels:
    {{- include "tracecat.labels" . | nindent 4 }}
  {{- with $baseAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          # API service
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-api
                port:
                  number: 8000
{{- if .Values.mcp.enabled }}
          # MCP server
          - path: /mcp
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          # MCP OIDC auth routes
          - path: /.well-known/oauth-authorization-server
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /.well-known/oauth-protected-resource
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /authorize
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /token
            pathType: Exact
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /register
            pathType: Exact
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /consent
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
          - path: /auth/callback
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-mcp
                port:
                  number: {{ .Values.mcp.port }}
{{- end }}
          # UI (default/catch-all)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "tracecat.fullname" . }}-ui
                port:
                  number: 3000
{{- end }}
{{- end }}
