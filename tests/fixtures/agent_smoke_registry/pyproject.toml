[project]
name = "agent-smoke-registry"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["local_registry*"]
