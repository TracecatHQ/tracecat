import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "MCP | Organization",
}

export default function MCPLayout({ children }: { children: React.ReactNode }) {
  return children
}
