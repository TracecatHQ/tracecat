type: action
definition:
  title: List device parameters
  description: List management parameter (compliance check) results for a device.
  display_group: Iru (Kandji)
  doc_url: https://api-docs.iru.com/#8ca8c0e9-654e-40c1-8d8c-c9a8a6e262c6
  namespace: tools.iru
  name: list_device_parameters
  secrets:
    - name: iru
      keys: ["IRU_API_TOKEN"]
  expects:
    base_url:
      type: str | None
      description: Iru (Kandji) API base URL (e.g. https://acme.api.kandji.io).
      default: null
    device_id:
      type: str
      description: Device UUID.
  steps:
    - ref: list_device_parameters
      action: core.http_request
      args:
        url: ${{ inputs.base_url || VARS.iru.base_url }}/api/v1/devices/${{ FN.url_encode(inputs.device_id) }}/parameters
        method: GET
        headers:
          Authorization: Bearer ${{ SECRETS.iru.IRU_API_TOKEN }}
  returns: ${{ steps.list_device_parameters.result }}
