type: action
definition:
  title: Get domain
  description: Retrieves a domain of the customer.
  display_group: Google Workspace Directory
  doc_url: https://developers.google.com/workspace/admin/directory/reference/rest/v1/domains/get
  namespace: tools.google_directory
  name: get_domain
  secrets:
    - type: oauth
      provider_id: google_admin
      grant_type: client_credentials
      optional: true
    - name: google_api
      keys: ["GOOGLE_API_CREDENTIALS"]
      optional_keys: ["GOOGLE_API_SUBJECT"]
      optional: true
  expects:
    customer:
      type: str
      description: >-
        The unique ID for the customer's Google Workspace account. In case of a
        multi-domain account, to fetch all groups for a customer, use this field
        instead of `domain`. You can also use the `my_customer` alias to
        represent your account's `customerId`.
      default: my_customer
    domain_name:
      type: str
      description: Name of domain to be retrieved.
  steps:
    - ref: get_domain
      action: tools.google_api.call_api
      args:
        service_name: admin
        version: directory_v1
        resource: domains
        method_name: get
        access_token: ${{ SECRETS.google_admin_oauth.GOOGLE_ADMIN_SERVICE_TOKEN }}
        scopes: ["https://www.googleapis.com/auth/admin.directory.domain.readonly"]
        params:
          customer: ${{ inputs.customer }}
          domainName: ${{ inputs.domain_name }}
  returns: ${{ steps.get_domain.result }}
