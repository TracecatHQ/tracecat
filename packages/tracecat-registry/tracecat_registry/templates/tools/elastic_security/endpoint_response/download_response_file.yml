type: action
definition:
  title: Download response file
  description: Download a response-action file as base64.
  display_group: Elastic Security
  doc_url: https://www.elastic.co/docs/api/doc/kibana/operation/operation-endpointfiledownload
  namespace: tools.elastic_security
  name: download_response_file
  secrets:
    - name: elastic_security
      keys: ["ELASTIC_API_KEY"]
  expects:
    action_id:
      type: str
      description: Response action ID.
    file_id:
      type: str
      description: Response file ID.
    params:
      type: dict[str, Any] | None
      description: Optional native Kibana query parameters.
      default: null
    base_url:
      type: str | None
      description: Kibana base URL (e.g. https://localhost:5601).
      default: null
    space_id:
      type: str | None
      description: Optional Kibana space ID. Omit for the default space.
      default: null
    verify_ssl:
      type: bool
      description: Whether to verify SSL certificates.
      default: true
  steps:
    - ref: build_path
      action: core.script.run_python
      args:
        inputs:
          path: /api/endpoint/action/${{ FN.url_encode(inputs.action_id) }}/file/${{ FN.url_encode(inputs.file_id) }}/download
          space_id: ${{ inputs.space_id }}
        script: |
          from urllib.parse import quote

          def main(path, space_id):
              if space_id:
                  return f"/s/{quote(space_id, safe='')}{path}"
              return path
    - ref: request
      action: core.http_request
      args:
        url: ${{ inputs.base_url || VARS.elastic_security.base_url }}${{ steps.build_path.result }}
        method: GET
        params: ${{ inputs.params }}
        verify_ssl: ${{ inputs.verify_ssl }}
        base64_encode_data: true
        headers:
          Authorization: ApiKey ${{ SECRETS.elastic_security.ELASTIC_API_KEY }}
  returns: ${{ steps.request.result.data }}
