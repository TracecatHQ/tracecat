type: action
definition:
  title: List IOC types
  description: >-
    List IOC and threat-type combinations. Calls POST /api/v1/ directly with query types.
  display_group: ThreatFox
  doc_url: https://threatfox.abuse.ch/api/#types
  namespace: tools.threatfox
  name: list_ioc_types
  secrets:
    - name: abusech
      keys: ["ABUSECH_API_KEY"]
  expects:
    base_url:
      type: str | None
      description: Base URL of the ThreatFox API.
      default: null
  steps:
    - ref: request
      action: core.http_request
      args:
        url: ${{ inputs.base_url || VARS.threatfox.base_url || "https://threatfox-api.abuse.ch" }}/api/v1/
        method: POST
        headers:
          Auth-Key: ${{ SECRETS.abusech.ABUSECH_API_KEY }}
          Accept: application/json
          Content-Type: application/json
        payload:
          query: types
  returns: ${{ steps.request.result }}
