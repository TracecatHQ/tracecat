type: action
definition:
  title: Get presentation
  description: Gets the latest version of the specified presentation.
  display_group: Google Slides
  doc_url: https://developers.google.com/workspace/slides/api/reference/rest/v1/presentations/get
  namespace: tools.google_slides
  name: get_presentation
  secrets:
    - type: oauth
      provider_id: google_slides
      grant_type: authorization_code
      optional: true
    - type: oauth
      provider_id: google_slides
      grant_type: client_credentials
      optional: true
    - type: oauth
      provider_id: google_admin
      grant_type: client_credentials
      optional: true
    - name: google_api
      keys: ["GOOGLE_API_CREDENTIALS"]
      optional_keys: ["GOOGLE_API_SUBJECT"]
      optional: true
  expects:
    presentation_id:
      type: str
      description: The ID of the presentation to retrieve.
  steps:
    - ref: get_presentation
      action: tools.google_api.call_api
      args:
        service_name: slides
        version: v1
        resource: presentations
        method_name: get
        access_token: ${{ SECRETS.google_slides_oauth.GOOGLE_SLIDES_USER_TOKEN || SECRETS.google_slides_oauth.GOOGLE_SLIDES_SERVICE_TOKEN || SECRETS.google_admin_oauth.GOOGLE_ADMIN_SERVICE_TOKEN }}
        scopes: ["https://www.googleapis.com/auth/presentations.readonly"]
        params:
          presentationId: ${{ inputs.presentation_id }}
  returns: ${{ steps.get_presentation.result }}
