type: action
definition:
  title: Get domain overview
  description: Get a domain overview. Calls GET /v1/overview directly.
  display_group: DomScan
  doc_url: https://domscan.net/docs
  namespace: tools.domscan
  name: get_domain_overview
  secrets:
    - name: domscan
      keys: ["DOMSCAN_API_KEY"]
  expects:
    params:
      type: dict[str, Any] | None
      description: API-native query parameters.
      default: null
    base_url:
      type: str | None
      description: Base URL of the DomScan API.
      default: null
  steps:
    - ref: request
      action: core.http_request
      args:
        url: ${{ inputs.base_url || VARS.domscan.base_url || "https://domscan.net" }}/v1/overview
        method: GET
        headers:
          x-api-key: ${{ SECRETS.domscan.DOMSCAN_API_KEY }}
        params: ${{ inputs.params }}
  returns: ${{ steps.request.result }}
