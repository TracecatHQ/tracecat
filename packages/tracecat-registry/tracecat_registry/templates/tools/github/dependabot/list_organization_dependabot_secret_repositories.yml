type: action
definition:
  title: List organization dependabot secret repositories
  description: 'List organization dependabot secret repositories. Calls GET /orgs/{org}/dependabot/secrets/{secret_name}/repositories directly. Accepted OAuth scopes: admin:org.'
  display_group: GitHub
  doc_url: https://docs.github.com/en/rest
  namespace: tools.github
  name: list_organization_dependabot_secret_repositories
  secrets:
  - type: oauth
    provider_id: github
    grant_type: authorization_code
  expects:
    org:
      type: str
      description: GitHub API path value for org.
    secret_name:
      type: str
      description: GitHub API path value for secret_name.
    params:
      type: dict[str, Any] | None
      description: API-native query parameters, including pagination and filters.
      default: null
    base_url:
      type: str
      description: GitHub.com REST API base URL.
      default: https://api.github.com
  steps:
  - ref: request
    action: core.http_request
    args:
      url: ${{ inputs.base_url }}/orgs/${{ FN.url_encode(inputs.org) }}/dependabot/secrets/${{ FN.url_encode(inputs.secret_name) }}/repositories
      method: GET
      headers:
        Authorization: Bearer ${{ SECRETS.github_oauth.GITHUB_USER_TOKEN }}
        Accept: application/vnd.github+json
        X-GitHub-Api-Version: '2022-11-28'
      params: ${{ inputs.params }}
  returns: ${{ steps.request.result }}
