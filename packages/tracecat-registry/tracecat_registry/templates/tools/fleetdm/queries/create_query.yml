type: action
definition:
  title: Create query
  description: Create a saved osquery query (report) in Fleet.
  display_group: Fleet
  doc_url: https://fleetdm.com/docs/rest-api/rest-api#create-query
  namespace: tools.fleetdm
  name: create_query
  secrets:
    - name: fleetdm
      keys: ["FLEETDM_API_TOKEN"]
  expects:
    base_url:
      type: str
      description: Fleet server base URL (e.g. https://fleet.example.com).
    name:
      type: str
      description: Name of the saved query.
    query:
      type: str
      description: The osquery SQL statement to save.
    description:
      type: str | None
      description: Human-readable description of the query.
      default: null
    platform:
      type: str | None
      description: Comma-separated platforms the query targets (e.g. darwin,windows,linux).
      default: null
    fleet_id:
      type: int | None
      description: Fleet (team) ID to scope the query to.
      default: null
    interval:
      type: int | None
      description: Schedule interval in seconds (0 or null for an unscheduled query).
      default: null
  steps:
    - ref: build_payload
      action: core.script.run_python
      args:
        inputs:
          name: ${{ inputs.name }}
          query: ${{ inputs.query }}
          description: ${{ inputs.description }}
          platform: ${{ inputs.platform }}
          fleet_id: ${{ inputs.fleet_id }}
          interval: ${{ inputs.interval }}
        script: |
          def main(name, query, description, platform, fleet_id, interval):
              payload = {"name": name, "query": query}
              if description is not None:
                  payload["description"] = description
              if platform is not None:
                  payload["platform"] = platform
              if fleet_id is not None:
                  payload["fleet_id"] = fleet_id
              if interval is not None:
                  payload["interval"] = interval
              return payload
    - ref: create_query
      action: core.http_request
      args:
        url: ${{ inputs.base_url }}/api/v1/fleet/reports
        method: POST
        headers:
          Authorization: Bearer ${{ SECRETS.fleetdm.FLEETDM_API_TOKEN }}
          Content-Type: application/json
        payload: ${{ steps.build_payload.result }}
  returns: ${{ steps.create_query.result.data }}
