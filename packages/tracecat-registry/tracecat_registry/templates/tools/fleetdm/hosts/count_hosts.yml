type: action
definition:
  title: Count hosts
  description: Count hosts matching the given filters without returning host records.
  display_group: Fleet
  doc_url: https://fleetdm.com/docs/rest-api/rest-api#count-hosts
  namespace: tools.fleetdm
  name: count_hosts
  secrets:
    - name: fleetdm
      keys: ["FLEETDM_API_TOKEN"]
  expects:
    base_url:
      type: str
      description: Fleet server base URL (e.g. https://fleet.example.com).
    status:
      type: str | None
      description: Host status filter (online, offline, new, mia, missing).
      default: null
    query:
      type: str | None
      description: Search string matched against hostname, IP, serial, and UUID.
      default: null
    fleet_id:
      type: int | None
      description: Scope the count to a fleet (team) ID.
      default: null
    policy_id:
      type: int | None
      description: Filter by hosts evaluated against this policy ID (pair with policy_response).
      default: null
    policy_response:
      type: str | None
      description: Policy compliance filter for policy_id (passing or failing).
      default: null
    software_title_id:
      type: int | None
      description: Only count hosts that have this software title installed.
      default: null
    vulnerability:
      type: str | None
      description: Only count hosts affected by this CVE (e.g. CVE-2024-1234).
      default: null
    mdm_enrollment_status:
      type: str | None
      description: MDM enrollment filter (manual, automatic, enrolled, pending, unenrolled).
      default: null
    os_settings_disk_encryption:
      type: str | None
      description: Disk encryption status filter (verified, verifying, action_required, enforcing, failed, removing_enforcement).
      default: null
    low_disk_space:
      type: int | None
      description: Only count hosts with less than this many GB of disk space.
      default: null
  steps:
    - ref: build_params
      action: core.script.run_python
      args:
        inputs:
          params:
            status: ${{ inputs.status }}
            query: ${{ inputs.query }}
            fleet_id: ${{ inputs.fleet_id }}
            policy_id: ${{ inputs.policy_id }}
            policy_response: ${{ inputs.policy_response }}
            software_title_id: ${{ inputs.software_title_id }}
            vulnerability: ${{ inputs.vulnerability }}
            mdm_enrollment_status: ${{ inputs.mdm_enrollment_status }}
            os_settings_disk_encryption: ${{ inputs.os_settings_disk_encryption }}
            low_disk_space: ${{ inputs.low_disk_space }}
        script: |
          def main(params):
              return {k: v for k, v in params.items() if v is not None}
    - ref: count_hosts
      action: core.http_request
      args:
        url: ${{ inputs.base_url }}/api/v1/fleet/hosts/count
        method: GET
        headers:
          Authorization: Bearer ${{ SECRETS.fleetdm.FLEETDM_API_TOKEN }}
        params: ${{ steps.build_params.result }}
  returns: ${{ steps.count_hosts.result.data }}
