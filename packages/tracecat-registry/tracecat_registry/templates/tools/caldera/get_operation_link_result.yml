type: action
definition:
  title: Get link result
  description: Retrieve the result payload for a specific Caldera link.
  display_group: MITRE Caldera
  doc_url: https://caldera.readthedocs.io/en/latest/Users/REST-api.html#operations
  namespace: tools.caldera
  name: get_operation_link_result
  secrets:
    - name: caldera
      keys:
        - CALDERA_API_KEY
  expects:
    operation_id:
      type: str
      description: Operation ID that contains the link.
    link_id:
      type: str
      description: Link ID to fetch.
    base_url:
      type: str | None
      description: Caldera API base URL (e.g. http://localhost:8888/api/v2).
      default: null
  steps:
    - ref: get_operation_link_result
      action: core.http_request
      args:
        url: ${{ inputs.base_url || VARS.caldera.base_url }}/operations/${{ inputs.operation_id }}/links/${{ inputs.link_id }}/result
        method: GET
        headers:
          KEY: ${{ SECRETS.caldera.CALDERA_API_KEY }}
          Content-Type: application/json
  returns: ${{ steps.get_operation_link_result.result.data }}
