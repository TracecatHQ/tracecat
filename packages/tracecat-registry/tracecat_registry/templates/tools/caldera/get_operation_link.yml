type: action
definition:
  title: Get operation link
  description: Retrieve a specific link from a Caldera operation.
  display_group: MITRE Caldera
  doc_url: https://caldera.readthedocs.io/en/latest/Users/REST-api.html#operations
  namespace: tools.caldera
  name: get_operation_link
  secrets:
    - name: caldera
      keys:
        - CALDERA_API_KEY
  expects:
    operation_id:
      type: str
      description: Operation ID that contains the link.
    link_id:
      type: str
      description: Link ID to fetch.
    base_url:
      type: str | None
      description: Caldera API base URL (e.g. http://localhost:8888/api/v2).
      default: null
  steps:
    - ref: get_operation_link
      action: core.http_request
      args:
        url: ${{ inputs.base_url || VARS.caldera.base_url }}/operations/${{ inputs.operation_id }}/links/${{ inputs.link_id }}
        method: GET
        headers:
          KEY: ${{ SECRETS.caldera.CALDERA_API_KEY }}
          Content-Type: application/json
  returns: ${{ steps.get_operation_link.result.data }}
