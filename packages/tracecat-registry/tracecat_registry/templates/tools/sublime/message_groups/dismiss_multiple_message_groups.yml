type: action
definition:
  title: Dismiss multiple message groups
  description: Dismiss multiple Sublime message groups.
  display_group: Sublime
  doc_url: https://docs.sublime.security/reference/dismissmultiplemessagegroups-1
  namespace: tools.sublime
  name: dismiss_multiple_message_groups
  secrets:
    - name: sublime
      keys: ["SUBLIME_API_KEY"]
  expects:
    payload:
      type: dict[str, Any]
      description: API-native JSON request body.
    base_url:
      type: str | None
      description: Base URL of the Sublime API.
      default: null
  steps:
    - ref: request
      action: core.http_request
      args:
        url: ${{ inputs.base_url || VARS.sublime.base_url }}/v0/message-groups/dismiss
        method: POST
        headers:
          Authorization: Bearer ${{ SECRETS.sublime.SUBLIME_API_KEY }}
        payload: ${{ inputs.payload }}
  returns: ${{ steps.request.result.data }}
