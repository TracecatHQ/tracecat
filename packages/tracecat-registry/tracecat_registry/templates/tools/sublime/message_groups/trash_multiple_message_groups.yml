type: action
definition:
  title: Trash multiple message groups
  description: Trash multiple Sublime message groups.
  display_group: Sublime
  doc_url: https://docs.sublime.security/reference/trashmultiplemessagegroups-1
  namespace: tools.sublime
  name: trash_multiple_message_groups
  secrets:
    - name: sublime
      keys: ["SUBLIME_API_KEY"]
  expects:
    payload:
      # Generic: this endpoint rejects an explicit null on optional body fields,
      # and a literal payload map cannot omit an unset optional.
      type: dict[str, Any]
      description: >-
        API-native request body. Documented fields: message_group_ids (required), classification, report_label, review_comment.
    base_url:
      type: str | None
      description: Base URL of the Sublime API.
      default: null
  steps:
    - ref: request
      action: core.http_request
      args:
        url: ${{ inputs.base_url || VARS.sublime.base_url }}/v0/message-groups/trash
        method: POST
        headers:
          Authorization: Bearer ${{ SECRETS.sublime.SUBLIME_API_KEY }}
        payload: ${{ inputs.payload }}
  returns: ${{ steps.request.result.data }}
