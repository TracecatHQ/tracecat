type: action
definition:
  title: List Gmail labels
  description: List all Gmail labels for the user
  display_group: Gmail
  doc_url: https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.labels/list
  namespace: tools.gmail
  name: list_labels
  secrets:
    - type: oauth
      provider_id: google_gmail
      grant_type: authorization_code
      optional: true
    - type: oauth
      provider_id: google_gmail
      grant_type: client_credentials
      optional: true
    - name: google_api
      keys: ["GOOGLE_API_CREDENTIALS"]
      optional_keys: ["GOOGLE_API_SUBJECT"]
      optional: true
  # Legacy: ported from the tools.gmail Python UDF. Inputs and output shape are a
  # published contract; do not rename or reshape them. Not a pattern for new actions.
  expects:
    user_id:
      type: str
      description: User ID or 'me' for authenticated user
      default: me
  steps:
    - ref: list_labels
      action: tools.google_api.call_api
      args:
        service_name: gmail
        version: v1
        resource: users.labels
        method_name: list
        access_token: ${{ SECRETS.google_gmail_oauth.GOOGLE_GMAIL_USER_TOKEN || SECRETS.google_gmail_oauth.GOOGLE_GMAIL_SERVICE_TOKEN }}
        scopes: ["https://www.googleapis.com/auth/gmail.readonly"]
        params:
          userId: ${{ inputs.user_id }}
  returns: ${{ steps.list_labels.result }}
